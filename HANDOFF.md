# HANDOFF — ashfall_vault

> Obsidian D&D vault for the "Ashfall Britannia" campaign (Tayls is a PLAYER, not the DM). Includes ddb-roll-sync and Site_HTML.
> Handoff is **enabled** for this repo. Every change updates the DO NEXT block below and prepends a log entry.
> Note: this is a notes/content vault — most session-note edits won't have a "next dev step." Use the DO NEXT block for things like next-session prep if useful, or leave it as "—".

## ▶ DO NEXT — Convert Session 12's `.docx`, then chase three DM answers.

> ✅ **S13–S15 ARE FULLY PROPAGATED AND PUBLISHING (2026-08-30, `4313b16`).** Convo 2 is done for all three. The site index went from **2 published sessions to 5**, and `all_sessions` from 12 to 15.
> ✅ **The website blocker is cleared.** `generate_public_session_index.mjs` reads `00-Campaign-Hub/Campaign Dashboard.md`, **not** `01-Sessions/` — its table stopped at S11, which is why nothing since June could publish. Rows for 13/14/15 are in, along with the **Quote Board / Loot Tracker / Profanity Ledger** blocks the generator requires for any session ≥10. ⚠️ **Sessions ≥10 need BOTH a Dashboard row AND all three tracker blocks, or the generator throws and writes nothing at all.**
> ✅ **The three-session roll-archive debt is CLEARED.** S13, S14 and S15 all queried via `mcp__supabase-cutter__execute_sql` on project `vtrtyagltwdrbastpppl`. ⚠️ **Two gotchas, both recorded in [[Roll Statistics S11-S20]]:** `individual_values` is a **JSON string** (`"[6]"`) not an array — parse with `(individual_values #>> '{}')::jsonb -> 0` or every session reports zero nat-20s; and the archive **undercounts d20s** because several players roll physical dice, so treat its counts as a floor.

**▶ Next actions, in order:**

1. 🛑 **Convert `01-Sessions/Session 12 — Only the Relics Can End Their Rule.docx` to markdown.** This is now **the single remaining blocker to a complete public record** — the index jumps S11 → S13 and the Dashboard has a visible gap. Use `Workflows/scripts/docx_to_text.py` (run `--selftest` first, it is **still unverified**). ⚠️ Convert the **approved `.docx`** — do *not* regenerate from `13_062926_corrected.md`, which would create a second divergent account. Then it needs a Dashboard row **and** S12 blocks in all three required trackers.
2. 🛑 **Three DM answers are blocking, and two of them block note amendments.** All are written up in [[DM Questions — Open]] under *S13–S15*:
   - **`Rite of the Dawning Radiance`** — S13 says [[Flux]] cast it as a ritual granting **Sunward Blessing**; S14 establishes it is **5th level** and she isn't taking it. **S13 note deliberately unamended.**
   - **[[Eiluned Lulu Denulie|Lulu]]'s subclass** — S14 records **Circle of the Land** in-fiction, S15 plays **Wildfire Druid**. **S14 note deliberately unamended.** Suggested fix: append *"— retconned in S15"* rather than rewriting.
   - **Do purity filters work on a corpse?** Deferred in S14, never answered. **It gates whether [[Deanna Smith-Wesson]] can be raised at all.**
3. ⚠️ **Check the Discord loot tab and reconcile two things** the DM promised in-session and never delivered aloud: the **obsidian tablet's contents** (S15) and the **gem values** for the 20 raw gems. The tracker records the DM's spoken list; **the tab is authoritative.**
4. 🛑 **PRIVACY — the standing policy decision is still open.** Three consecutive transcripts carried significant out-of-character personal information about real people; all redacted per per-session rulings, unredacted originals in gitignored `_pipeline/`. **Standing recommendation: gitignore `Session_Sources/Transcripts/Raw_Unedited/` the way `Recordings/` already is.** ⚠️ **17 transcripts are already public and have NOT been audited.**
5. ⚠️ **Website (`rectrixcaedere`) — confirm the live site picked S13–S15 up.** It fetches `Public Session Index.json` from this repo's `main` at request time, so the push should be enough (~5 min CDN). ⚠️ Ashfall's `sessionRollsOnly` stays **OFF** in `app.js` until the **S01/S02/S04 index-date off-by-one** is fixed — enabling it now would silently drop ~1,037 real rolls. ⚑ **S13 has the same off-by-one** (rolls keyed 08-03 for an 08-02 session); S14 and S15 are correct.
6. ⚑ **Ask the DM: who is the cloaked telepath?** (carried from S11, still open.) *"you led me on a delightful chase… I can't wait till next time."*
7. 🛑 **Add a lock to `Workflows/scripts/ashfall_pipeline_watch.js`.** A duplicate concurrent run fired during S11 Phase B and wrote two artifacts under the primary run.
8. ⚑ **Vault conventions worth a decision:** there is **no Items folder** ([[Vault Format Reference]] defines PCs/NPCs, Locations and Creatures only), so relics live in the Loot Tracker and `[[Blade of Sunfire]]` / `[[The Sun Shard]]` are de-linked or dangling. Relics are accumulating — the Sun Shard, the Blade of Sunfire, and now Vega's unnamed cursed longsword. ⚑ Also: **`[[Session 12]]` has no markdown target**, so it is written as plain bold everywhere.

⚠️ `_pipeline/` is gitignored — review packets, unredacted transcripts and Convo 2 handoffs live only on this machine.

---

### Standing work order (still pending, from the 2026-07-04 vault audit)
1. ⚡ **Check the recording app** (standing flag from Session 09: only Taylor's mic captured, no Discord audio). Test before Monday.
2. **Claude Code migration (from 2026-07-04 audit):** unify 10 session notes to the adopted superset schema — add `type: session`; rename `session:` → `session_number:` and `date:` → `session_date:`; kebab keys → snake (`party-level` → `party_level`, `roll-archive-date` → `roll_archive_date`). Ashfall notes carry no `type:` today, so this vault's DnD.base Sessions view currently matches ZERO notes.
3. **Publish command (new):** `Workflows/scripts/Publish-Ashfall.cmd` commits + pushes all note changes; the site fetches session notes straight from this repo's main, so push = publish (~5 min CDN).
4. **Inside Obsidian (never shell):** create `Templates/` and move the six root template stubs into it (they show as phantom blank rows in DnD.base); create `06-Media/` and move the two battle-map PNGs at root into it; move the `ddb-roll-sync/` extension folder under `Workflows/` to match SITL/WTFF layout.
5. **Parity items:** add `Workflows/_Workflows Index.md` and an `Onboard Ashfall Britannia.md` (mirror the SITL/WTFF versions).

---

## Log
<!-- newest first · one entry per logical task/session · timestamp · source · changed · commit · next -->

### 2026-08-30 17:10 ET · Claude Code (Convo 2 — S13–S15 propagated, site unblocked)
- **Changed:** Propagated S13, S14 and S15 across the whole vault. **11 new pages** — [[Eiluned Lulu Denulie]] (PC, joined S14), [[Marrowin Quill]] (NPC), [[Boilborn]] and [[Bonewraith]] (creatures), and [[Lantern Spine]], [[Ossuary Approach]], [[Old Shrine]], [[Broken Descent]], [[Grate of the Veins]], [[Watcher's Pit]] and [[Flooded Gallery]] (locations; the last two were **never visited** and get pages so the skipped route stages don't vanish). **[[Deanna Smith-Wesson]] marked DEAD** with a *Death & Revival* section — both revival routes, her effects tracked to their new owners, and the observation that **[[The Beast Tamer]]'s true name died with her.** Key Events appended for the six surviving PCs. **S13/S14/S15 blocks added to all four trackers.** Campaign Dashboard: three session rows, NPC directory, the whole catacomb route under Locations, timeline, a rewritten Party Snapshot, and **19 new Active Threads (68–86)**. [[DM Questions — Open]] gained an S13–S15 section.
- **Commit:** `4313b16`
- **Verification:** ✓ `generate_public_session_index.mjs` self-test passes and the live run went from **2 published sessions to 5** (S10, S11, S13, S14, S15), `all_sessions` 12 → 15. ✓ All three notes carry 5/5 parser-contract headings. ✓ All 11 new pages exist on disk. ✓ Roll archive queried for all three sessions and every swing roll cross-checked. ✓ Staged diff scanned clean for sensitive strings.
- **Friction:** misread — I wrote `[[Blade of Sunfire]]` and `[[Session 12]]` as wikilinks before checking [[Vault Format Reference]], which defines folders for **PCs/NPCs, Locations and Creatures only — there is no Items convention** — and S12 exists only as a `.docx`. ✅ **What worked: read `Vault Format Reference.md` BEFORE creating pages, not after.** De-linked both to bold; the de-link then produced malformed `****text**` where the link had already sat inside bold, caught by grepping for `\*\*\*\*` and fixed.
- **Friction:** gen-fail — my first broken-wikilink checker split on `|` and not `\|`, so it reported 44 "unresolved" links that were really the vault's normal `[[Page\|Alias]]` table form. ✅ **What worked: check a sample of the reported failures by hand before acting on a link audit** — only 4 of the 44 were real, and 2 of those were mine.
- **Watch out:** 🛑 **No S12 Dashboard row was added on purpose** — S12's note is `.docx`-only, and a row without a markdown note **makes the generator throw and write nothing at all**, taking the whole site index down with it. Convert the `.docx` first. 🛑 **Neither the `Rite of the Dawning Radiance` nor the Lulu-subclass contradiction was retro-edited** — both need the DM. ⚠️ **[[Deanna Smith-Wesson]] has zero archived rolls for S13** despite killing a Bonewraith in it.
- **Next:** Convert Session 12's `.docx` so it can take a Dashboard row, and chase the three blocking DM answers.

### 2026-08-30 15:45 ET · Claude Code (S13–S15 backlog cleared)
- **Changed:** Wrote and banked **three session notes**, clearing the two-month backlog. **S13 — The Cartographer of the Underkeep** (`418922c`). **S14 — An Angel with a Shotgun** (`8f642d1`): the campaign's **first PC death**, [[Deanna Smith-Wesson]] killed by a tunnel collapse on a natural 1, unrevivable because the bypass was a burial ground for cursed people (7 curses fused into one); replacement PC **[[Eiluned "Lulu" Denulie]]** introduced the same night. **S15 — Only Those Who Bleed Fire** (`59e32ee`): the party takes the relic — a cursed burning +1 longsword [[Vega Bloodroot]] pulls from a stone after her palm splits open — and fights the **Boilborn**, homebrew the DM built expressly as an anti-tank aimed at Vega. Also added an `aliases` block to `Zelda Z Whipper.md`.
- **Commit:** `418922c`, `8f642d1`, `59e32ee`
- **Verification:** ✓ All 5 parser-contract headings present in both new notes. ✓ Every swing roll cross-checked against `ashfall_session_rolls` and **all corroborate** — including Deanna's last roll ever (**Dex save 1d20+5, natural 1, total 6**, 00:53:58 UTC) and four natural 1s inside three minutes opening S15. ✓ Redaction scripts assert expected content per line and abort before writing on any mismatch; both ended **ALL CHECKS PASSED**, both are idempotent (re-run clean via skip-guards). ✓ Staged diffs scanned for sensitive strings before every commit; `UNREDACTED` copies confirmed gitignored.
- **Friction:** gen-fail — the S15 transcript wrote itself to `082726 Ashfall Recording_transcript.md` instead of the `15-082726_raw_transcript.md` convention, because `transcribe.js` derives the name from the recording filename and this one lacks "Brittania". Renamed by hand; **worth fixing in the script** so the next odd filename doesn't silently break the convention.
- **Friction:** misread — I recorded the new PC's name as "Ilunid Danuli" from audio at 60% confidence and nearly shipped it. The **D&D Beyond roll archive** has her character sheet: **Eiluned "Lulu" Denulie**. ✅ **What worked, and reuse it: the roll archive is a spelling oracle for PC and creature names — it outranks the transcript, and it lifts the 60% first-seen cap.**
- **Friction:** misread — `_pipeline/S14/flags.md` mis-categorised one of the redacted household names; S15 shows it is the DM's **dog**, not a person. Redaction stands, reasoning was wrong. Corrected in the S15 note rather than by silently editing the S14 packet.
- **Friction:** misread — my first S14 redaction pass **missed three lines of pre-game chat naming a player's child**, and my own redaction marker *enumerated the categories* of what had been removed, which republishes the thing the redaction protects. Both were caught **only by scanning the outgoing `git diff` before pushing, not by re-reading the file**. ✅ **What works, do it every time: (1) scan `git log -p origin/main..HEAD` for sensitive strings as the last step before `git push`, not just the working tree; (2) redaction markers and note text say *that* something was removed and *where the detail lives*, never *what it was*.** Unpushed commits were rebuilt from `origin/main` so the leaked wording never entered public history.
- **Watch out:** 🛑 **Two contradictions are recorded but deliberately NOT fixed** — `Rite of the Dawning Radiance` (S13 vs S14) and **Lulu's subclass** (S14 Circle of the Land vs S15 Wildfire Druid). Both need the DM; neither note was retro-edited. See DO NEXT item 4. ⚠️ `[[Zelda "Z" Whipper]]` was a **broken link in 10 files** (the page is `Zelda Z Whipper.md`, no frontmatter, no aliases) — fixed at source rather than by rewording the new notes. ⚠️ **9 pages the new notes link still do not exist** — that is Convo 2 work, not invented here.
- **Next:** Convo 2 for S13–S15, then Campaign Dashboard rows + tracker blocks — **without which none of this reaches the website.**

### 2026-08-30 12:20 ET · Claude Code (all_sessions; two data problems found)
- **Changed:** `generate_public_session_index.mjs` now also emits **`all_sessions`** — every session, number and date only (12 entries, including 04.5). The curated `sessions` list is unchanged. This was needed because **rectrixcaedere's `dashboard.html?c=3` was reporting "SESSIONS LOGGED: 2"** while showing 1,681 live rolls: it read the count from `sessions`, which only carries S10+. **It now correctly reads 12.**
- **Commit:** `7c2c99c` (site side: `rectrixcaedere@330a8bb`)
- **Verification:** ✓ Self-test passes. ✓ Confirmed on the live domain: Ashfall dashboard **1,681 rolls / 12 sessions**, was 2.
- **Watch out:** 🔴 **Two genuine data problems surfaced while checking this — neither is fixed, and both need Taylor.**
  **(1) The index's session dates are off by one day from the roll dates for S01, S02 and S04.** The index says S01 = **2026-02-13**; the archive has **93 rolls on 2026-02-12** and none on the 13th. Same shape for S02 (index 02-17, rolls 02-16, 125 rolls) and S04 (index 03-04, rolls 03-03, 81 rolls). Cause: `generate_public_session_index.mjs` reads dates from **`Campaign Dashboard.md`**, while the site's own hardcoded `ARC` uses the **roll** dates — and `ARC` has `d:'2026-02-12'` for S01. ⚠️ **This means per-session roll tallies on `ashfall-britannia/session.html` and `archive.html` are querying the wrong day for those three sessions** and will be showing zero or wrong counts. Fix the Dashboard dates, or teach the generator to prefer the roll date.
  **(2) ~400 rolls exist for sessions the vault has no record of.** `ashfall_session_rolls` holds rolls on **2026-08-03 (63), 2026-08-17 (103) and 2026-08-27 (235)** — all **after S11 (2026-06-22)**, the last session in the Campaign Dashboard. There are also older orphans: 2025-11-08, 2025-11-12 (**196 rolls**), 2026-04-15, 2026-06-04, 2026-06-08 (95), 2026-06-29. Either sessions 12+ have been played and never written up, or the roll sync is pulling from another campaign. **Worth checking before the next session note is written.**
  ⚠️ Because of both, **`sessionRollsOnly` is deliberately OFF for Ashfall** in `app.js` — enabling it would have silently dropped **1,037 real rolls** from the dashboard. SITL has it on. Turn Ashfall's on only after the dates reconcile.
- **Next:** Unchanged — see the DO NEXT block above.

### 2026-08-29 17:40 ET · Codex
- **Changed:** Added the generated `Public Session Index.json` contract and validator. Convo 2 now fails closed if a new session lacks a Markdown note, required public headings, or Quote/Loot/Profanity blocks; Rectrix consumes the pushed index live.
- **Commit:** `08637d3`
- **Next:** Push this vault commit, then verify Rectrix loads Sessions 10 and 11 from the raw index; dashboard roll-data automation awaits Taylor’s explicit public-data approval.
- **Watch out:** S12 is still a `.docx`, so it is intentionally not eligible for the public index. Legacy S01–S09 parser defects are recorded in the index but do not block new S10+ publication.

### 2026-08-29 00:40 ET · Claude Code (phantom-cause correction)
- **Changed:** Rewrote the `▶ DO NEXT` header and block to retract the "the transcriber's work list is the defect" conclusion, which was **wrong**, and replaced it with an ordered, actionable list. Struck (did not delete) the two load-bearing false claims so the reasoning stays auditable.
- **Commit:** see next commit on this branch
- **The correction:** ✅ **Taylor confirmed she dropped `062226` and `062926` into `Recordings/` by hand**, to test the `Ashfall Pipeline Watcher` — registered for the first time that evening (`ca0357c`) after being found never-installed. 🛑 **There is no work list, queue, or scheduler to fix.** `ashfall_pipeline_watch.js:358` is a bare `chokidar.watch(RECORDINGS, { ignoreInitial: true })` on `add`; `nextSession()` (line 172) takes the **date** from the filename's first 6-digit run and the **number** from `max("Session NN" in 01-Sessions/) + 1`. **Any** `.mp3` dropped now becomes "Session 13" by construction, and re-dropping processed audio yields a duplicate every time. Two runs, two duplicates, zero defects. ⚠️ Also corrected: duplicates in `Raw_Unedited/` **cannot** "confuse the watcher" — `nextSession()` never reads that directory.
- **Verification:** ✓ Read from source, not inferred: the chokidar options block, the `nextSession()` body, and `SESSIONS_DIR`/`RAW_DIR` constants. ✓ Confirmed the numbering ceiling is 12 **because `Session 12 — ….docx` matches the `Session\s+(\d+)` filename regex** — so item 0b's `.docx` problem and the numbering problem are one problem, not two. ✓ Confirmed `080226`, `081726`, `082726` have **no** transcripts in `Raw_Unedited/`.
- **Friction:** misread — two consecutive sessions escalated "the transcriber's work list is the defect" into the `DO NEXT` block and banked it, without reading `ashfall_pipeline_watch.js` to check that a work list exists. It does not. The evidence-gathering underneath was excellent and all of it survived; only the causal claim was wrong. **What settled it in one step was reading the 12 lines of `nextSession()` and the chokidar options** — cheaper than the three independent duplicate-verifications that were run instead. **When a conclusion names a mechanism, open the mechanism before writing it into DO NEXT.**
- **Watch out:** ⚠️ The 2026-08-29 01:20 entry below still contains the retracted sentence *"the transcriber's work list is the defect, not the sessions."* Log entries are history and were left as written; the `DO NEXT` block is the corrected source of truth. ⚠️ This Log is now at 16 entries, one over the 15 cap — not rotated here, since rotating as a side effect of another change is the failure mode `septentrion` logged repeatedly.
- **Next:** Convert `Session 12 — Only the Relics Can End Their Rule.docx` to markdown (`Workflows/scripts/docx_to_text.py`) — it unblocks site rendering and releases the session numbering in one move.

### 2026-08-29 01:20 ET · Claude Code
- **Changed:** Ran **Convo 2 (vault propagation)** for the "S13" 062926 handoff and **refused to propagate**, deliberately. Re-verified the duplicate finding **a third time from the raw files rather than inheriting it** — identical `# Source: 062926 Ashfall Recording.mp3`, 123-min duration, `universal-3-5-pro` model, identical first line of play (`Test, test.`) and last line (`Bye.` at `[02:02:44] SPEAKER G`); 1,836/15,243 vs 1,820/15,213 lines/words is ordinary ASR variance. 🛑 **No session note, no Dashboard row, no tracker blocks, no journal entry, no NPC/location/glossary/House-Rules edits** — all 14 checklist rows ruled ➖, because writing them would have duplicated S12's quotes, loot, profanity totals, rolls and worldbuilding under a false session key **and pushed it to the public repo `session.html` reads at request time** (**No Session Contamination**). **Second phantom session in two days — the transcriber's work list is the defect, not the sessions.** Per *Existing Notes Handling*, **audited S12 instead** and found its propagation **genuinely absent, not merely unmarked**: a vault-wide grep returns **no Dashboard row** (table stops at S11), **no `## S12` block in any of the four `S11-S20` trackers**, no journal entry, no PC Key Events. 🛑 **Could not fill the gap — blocked, not declined:** the only non-inventive source is the approved `.docx`, the sandbox refused every attempt to execute an extractor, and Convo 2 may not re-read transcripts. **Nothing invented.** Wrote the ruling into `00-Campaign-Hub/Vault Sync Status.md` (evidence table, disposition table, S12 audit, catch-up-queue row) and **corrected a misleading "CATCH-UP QUEUE EMPTY 🎉"** that was hiding S12 as the real blocker. Created `Workflows/scripts/docx_to_text.py` to unblock it.
- **Commit:** `b747e19`
- **Friction:** gen-fail — **the sandbox blocked script execution outright this time, not just awkwardly.** `python <script> <args>` was refused in **both** Bash and PowerShell (bare `python --version` runs fine, so it is the arguments, not the interpreter); `Remove-Item` and `Out-File` were refused as *"multiple operations"* / unvalidatable pipeline paths; `cd … && … > file` was refused for redirection; `git commit` heredoc was refused again. Worked by: **`Write` tool for all file creation** (never shell redirection), **`git commit -F <msgfile>` via PowerShell with `git -C <path>`** instead of `cd`, and **accepting that the `.docx` simply could not be read** rather than reaching for the transcript as a substitute. ⚠️ **This is the fourth session with the same pattern — the previous entry's advice ("use PowerShell for git, Bash only for single-verb file ops") held, but escalate it: assume ANY script execution with arguments will be refused, and design the run so the deliverable never depends on one.**
- **Next:** DO NEXT **updated in place, not replaced.** Item **0-NEW** now records that Convo 2 ran and refused. Item **0b promoted to 🛑 and rewritten**: S12's gap is now *audited and confirmed*, with the new `docx_to_text.py` converter and exact run commands attached. Item **1** records the Supabase re-test and adds `'2026-06-29'` to the backfill query. Items 0, 2, 3, 4, 5 and the standing work order left untouched.
- **Watch out:** ⚠️ **`Workflows/scripts/docx_to_text.py` is UNVERIFIED** — the sandbox blocked even its own `--selftest`. Run `python Workflows/scripts/docx_to_text.py --selftest` before trusting it on the real `.docx`. ⚠️ **Two phantom sessions in two days is a pipeline defect** — fix what feeds `transcribe.js` its work list before running it again; the genuine S13 (~070626) **still has no recording anywhere in the vault**. ⚠️ **Three duplicate raw transcripts now sit in `Raw_Unedited/`** (`13-062226`, `13-062926`, alongside the real `11-` and `12-`) and will confuse the watcher — not renamed or deleted, Taylor's call. ⚠️ `_pipeline/state.json` still points at the wrong transcript, so `--approve` will pick up the wrong file as-is.

### 2026-08-29 00:05 ET · Claude Code
- **Changed:** Ran **Convo 1 Phase B** on the approved S13 table. **Re-verified the duplicate finding independently rather than inheriting it** — identical `# Source: 062926 Ashfall Recording.mp3`, identical 123-min duration, identical first line of play (`Test, test.`) and last line (`Bye.` at `[02:02:44] SPEAKER G`), 0.2% word delta (15,243 → 15,213). It is Session 12's audio. **Applied all 15 approved rows in 16 edit ops across 14 lines** (rows 6/12/13 span more than one line or share a line), line-scoped exact-substring with each old string verified to occur exactly once on its approved line before replacing and re-verified after — **stricter than a global word-boundary regex, partial-word corruption impossible by construction.** Integrity: **1,820 lines in and out, +3 words matching the expected arithmetic exactly, zero residual old strings, 18 corrected forms present.** Wrote `Corrected/13_062926_corrected.md` and `Spell_Check_Logs/062926_Spell_Check_Log_RERUN.md`, and rewrote `_pipeline/S13/handoff.md`. 🛑 **Did NOT create `01-Sessions/Session 13 — ….md`** — there is no Session 13 in this audio, so writing one would have meant inventing a session and auto-publishing it to the public repo the live site reads. Same disposition Taylor set for the 062226 duplicate in `4039c47`. 🛑 **Supabase failed again** — `mcp__supabase__execute_sql` returned `Unauthorized`, both fallbacks needed permission grants a non-interactive run can't answer; **neither roll query ran and no rolls were fabricated.**
- **Commit:** `1edcb0a`
- **Friction:** gen-fail — **the sandbox blocked every scripted path to applying the corrections.** `python <script>` was refused in **both** Bash and PowerShell, `Copy-Item` was refused as *"multiple operations"*, and `git commit` was refused in Bash in **three** forms (heredoc `-F -`, `-F <file>`, and multi-`-m`). Worked by: **`cp` in Bash for the file copy**, **16 individual `Edit` tool calls** against the copied file instead of a script (slower, but each one is independently verified by the tool and the integrity check caught that nothing drifted), and **PowerShell + `git commit -F _pipeline/S13/commitmsg.txt`** for the commit — the same trick the last two entries recommend. ⚠️ **Confirmed pattern across three sessions now: use PowerShell for git, Bash only for single-verb file ops, and never assume an interpreter is available.**
- **Next:** DO NEXT **updated in place, not replaced** — item **0-NEW** now records that Phase B ran and what it produced, plus a new uncovered dollar artifact at L1316 (`add $100 to this`) left unapplied under No Silent Fixes. Item **0b escalated**: S12 being `.docx`-only means it **cannot render on the public site at all**, and the fix is to convert the approved `.docx`, *not* to regenerate from the rerun transcript (that would create a divergent second account). Items 0, 1, 2, 3, 4, 5 and the standing work order left untouched.
- **Watch out:** ⚠️ **Two known defects still sit in `12_062926_corrected.md`** — `Elandra` at L488 and the `$25/$50` artifact at L1218. They are fixed **only** in the rerun transcript; amending an already-propagated artifact was left as Taylor's call. ⚠️ **`Dawnwalker` is now provably unresolvable from audio** — both ASR runs produce `Cirrus` (444) / `Sirus` ×2 (458) / `Ceres` (488) in the same three places, so the variance is **in the DM's reading, not the transcription**. It needs the DM's written lore; a third transcription pass will not settle it. ⚠️ Supabase is now **six-plus consecutive automated runs with zero roll data**; the one-line fix (`SUPABASE_ACCESS_TOKEN=sbp_…` in the gitignored `.env`) is pinned in item 1 and remains the highest-value unblock in the repo.

### 2026-08-28 23:15 ET · Claude Code
- **Changed:** Ran **Convo 1 Phase A** on `13-062926_raw_transcript.md` and found it is a **duplicate — the second in a row.** It is a second AssemblyAI run (2026-08-29) of `062926 Ashfall Recording.mp3`, already processed as **Session 12** from a 2026-08-03 run of the identical file. Verified from the raw files rather than the header alone: same `# Source:`, 123 min, `universal-3-5-pro`, and a **full normalized line-by-line diff returning only ASR word variance and diarization turn-boundary shifts.** No new content anywhere. Banked as evidence; **no session note, no Supabase query, no Convo 2 work, nothing written outside `_pipeline/S13/`.** Ran the spell-check pass anyway and wrote it up **as a second opinion on the S12 transcript**, which is where its value is: **15 proposed corrections (5 at/under 60%), 25 flags.** 9 restate rows Taylor approved 2026-08-03 whose defects survive this run; **4 are new, and two of those close gaps the S12 spell-check log itself flagged as uncovered and that still sit in `12_062926_corrected.md`** — the **fourth** spelling of the queen (`Elandra`→`Alandra`, L488) and a dollar-sign ASR artifact (L1218). One row (`Blessings of Erona`/`Arona` → `Aurona`) the re-run **strengthens** 45% → 60%, because the two runs disagree with each other but agree on `Aurona` at L444. Two S12-approved rows needed no fix at all — the second pass got them right. ✅ **The one real gain: two independent diarization passes produce the same A–H speaker map, no letter changing owner** — corroboration for a blocker standing three sessions, though not confirmation, and script-format conversion stays blocked.
- **Commit:** `aa62857`
- **Friction:** gen-fail — **the Bash tool refused four shapes this session**, each costing a round trip: `diff <(…) <(…)` (*process_substitution*), a compound `cd … && sed … && diff` (*multiple operations*), a `for` loop with `${f}` (*simple_expansion*), and `git -C … status` (*untrusted-hook / approval*). PowerShell then rejected a `Compare-Object` one-liner as a *"script block that may execute arbitrary code"*. Worked by: **running `diff` on the two raw files directly with no pipeline or substitution** (the timestamp noise turned out to be tolerable and the diff was more readable than a normalized one), using the **Grep tool with an alternation pattern** instead of shell greps for line-number lookups, and **PowerShell — not Bash — for `git status` / `git commit`**. ⚠️ The prior entry's `_pipeline/S13/commitmsg.txt` + `git commit -F` trick worked first try again; keep using it, `_pipeline/` is gitignored so the scratch file never pollutes anything.
- **Friction:** misread — I nearly overwrote `_pipeline/S13/{spellcheck,flags,summary}.md` from the **previous** S13 run without checking, and `git ls-files` showed `_pipeline/` is **untracked/gitignored**, so there would have been no recovery. Worked by copying all three to `*_062226_PRIOR_RUN.md` first. ⚠️ **Standing hazard: `_pipeline/S##/` is reused by session number, and session numbers are currently wrong twice over.** Any future run that reuses a numbered pipeline folder should archive before writing.
- **Next:** DO NEXT **changed in substance** — item **0-NEW** added above the old item 0 (both duplicates now documented, and the fix reframed as *upstream*: look at what feeds `transcribe.js` its work list). Item **3 rewritten** — it claimed S12's Phase A hadn't been run, which is long stale; it now points at the two known defects still in the corrected S12 file that item 0-NEW's corrections would fix. Items 0b, 1, 2, 4, 5 and the standing work order left untouched.
- **Watch out:** 🛑 **Two consecutive false sessions is a pipeline defect, not a coincidence** — S13's slot has now held a re-run of S11's audio *and* a re-run of S12's audio. **The real Session 13 (~070626) has never been transcribed.** ⚠️ `_pipeline/state.json` still points at the **062226** transcript with `"stage": "complete"` — **deliberately not edited** (valid stage values are undocumented; guessing one is invention), but `--approve` will grab the wrong file as-is. ⚠️ **`_pipeline/` is gitignored** — every review packet in it exists only on this machine. ⚠️ The OOC block at L1682–1696 of this recording (the DM's stated reason for fading to black) is **personal** and must not reach any public-facing panel; the site parser publishes from this repo. ⚠️ `⟦FILL: player languages / accents⟧` is now **unfilled for a 7th session** and `⟦FILL: Vega voice guide⟧` for a **6th** — the second one will block Section 2 of the next real session's notes.

### 2026-08-28 22:22 ET · Claude Code
- **Changed:** Ran **Convo 2** for the "S13" handoff and **refused the propagation** — there is no Session 13. Re-verified the duplicate claim from the raw files rather than taking the handoff's word for it (identical `# Source:` mp3, 137 min, `universal-3-5-pro`, 95.1%, same first line of play), then followed `Project_Instructions.md` → *Existing Notes Handling* and **audited S11's propagation instead of writing a new session**. **Audit result: complete, zero gaps** — `## S11` blocks in all four `S11-S20` trackers, `**S11` Key Events bullets on **all 7** PC pages, plus journal, Dashboard and glossary. Writing a Session 13 would have duplicated S11's 60 quotes, 17 loot rows, profanity running totals and worldbuilding under a false session key **and pushed it to the public repo `session.html` reads live** — the No Session Contamination constraint. **4 files changed, all surgical annotations, no rewrites:** `Names & Terms.md` (**`Hunter` struck and flagged as a candidate ASR hallucination** — 3 occurrences in run 1, **zero** in run 2 of the identical audio; entry preserved, not deleted, so the evidence trail survives — retirement stays Taylor's call), `Session 11 — A Delightful Chase.md` (+2 Archivist Notes: the full two-run cross-check, and the **disputed "Pins and Needles" attribution**), `Zelda Z Whipper.md` (her authorship of that ability flagged unsettled), and `Vault Sync Status.md` (the "S13" ruling with a full ➖ matrix and the reason per row, the change-log entry, three new catch-up rows, and **S11's Supabase diagnosis corrected in place**).
- **Commit:** `4039c47`
- **Friction:** gen-fail — **the Bash tool refused five different shapes this session** and each one cost a round trip: `python -c` inside a compound command (*multiple operations*), `git commit -F` in a compound command (*multiple operations*), `git diff > file` after `cd` (*redirection with cd*), `$VAR`/`${#VAR}` tests (*simple_expansion*), and `cmd && echo || echo` (*subshell*). PowerShell then rejected the `@'…'@` here-string commit as **"too long for parsing (1327 bytes, max 965)"** — the exact technique the previous entry recommended. Worked by: **writing the commit body to `_pipeline/S13/commitmsg.txt` and running `git add -A; git commit -F <file>` in PowerShell with `;` separators**, and using the **Grep tool instead of piping to grep** for the secret scan. ⚠️ Note for next time: `_pipeline/` is gitignored, so a message file there is a free scratchpad that never pollutes the commit.
- **Friction:** gen-fail — `mcp__supabase-account2__list_projects` raised an un-answerable permission prompt in a non-interactive run, same dead end as the last three sessions. The fallback servers remain useless here until they're allowlisted; **don't spend turns on them.**
- **Next:** DO NEXT is **unchanged in substance** — item 0 (resolve the duplicate transcript, find the real S13) is still the blocking decision and still Taylor's. **Item 1 was edited surgically:** the Supabase fix is now pinned to a single line in `.env`.
- **Watch out:** ⚡ **The five-run roll-archive blocker is now fully diagnosed, and the previous entry's theory was still one step short.** It is not the allowlist (confirmed — the call raised **no** permission prompt) and not a mystery server setting: `.mcp.json` interpolates `"SUPABASE_ACCESS_TOKEN": "${SUPABASE_ACCESS_TOKEN}"`, and **the variable is unset**, so the server starts with an empty token and answers `Unauthorized`. `.env` holds only `DDB_COBALT`. **Add one line to `.env` and the backlog clears in a single interactive pass** (S10 · S11 · S12 dates all at once). ✓ Verified while checking: `.env` is gitignored and `.mcp.json` carries no inline secret. 🛑 **S12 is the real upstream blocker, not S13** — `.docx`-only in a markdown folder, format retired 06/06/2026, **no markdown note and no `## S12` block in any tracker**. ⚠️ `_pipeline/state.json` still points at the phantom S13 (`stage: awaiting_approval`) and will keep the watcher stuck — **deliberately not edited**, because the state-machine's valid stage values aren't documented anywhere and guessing one would be invention. ⚠️ Website still un-updated for **S10, S11 and now S12**. ⚠️ `⟦FILL: player languages / accents⟧` (6th session) and `⟦FILL: Vega voice guide⟧` (5th) still unfilled.

### 2026-08-28 22:12 ET · Claude Code
- **Changed:** Ran Convo 1 **Phase B** on the mislabeled `13-062226` transcript and **again stopped short of the session note.** Applied the Taylor-approved corrections — **25 of 29 rows in 27 edit ops** — to `Corrected/13_062226_corrected.md`, each search string verified unique before applying and re-verified by grep after; the file is intact at 2,027 lines (1,996 + a 31-line identity banner). The **4 held/not-recommended rows (L20, L1164, L1238, L1540) were left as transcribed**, per the approved table's own instruction, and confirmed still present afterward. 🛑 **Did NOT write the log to the requested `062226_Spell_Check_Log.md`** — that filename holds **S11's Taylor-approved log** (28 rows · 25 applied · 3 held, approved 2026-08-03) and overwriting it would have destroyed an approved artifact for a different session; filed as `062226_Spell_Check_Log_RERUN.md` instead. 🛑 **Did NOT create `01-Sessions/Session 13 — ….md`.** The approved spellcheck table's own header says *"Nothing below should be applied to a new 'Session 13'"*, and `01-Sessions/` auto-commits to a **public** repo that `session.html` reads live — a duplicate session dated 06/22/2026 would have been published before Taylor could look at it. Wrote `_pipeline/S13/rerun_delta.md` (what this run actually changes about the S11 record) and `_pipeline/S13/handoff.md`, whose first line points at the real note for this audio, `01-Sessions/Session 11 — A Delightful Chase.md`.
- **Commit:** `0cf43e7`
- **Friction:** gen-fail — **the shell was locked down harder than in any prior session, and it cost a whole approach.** `node <script>`, `Copy-Item`, `git -C … add`, a bash heredoc `git commit -F -`, and a multi-line `git commit -m` in Bash were each refused. I had written `_pipeline/S13/apply_corrections.js` to do all 27 replacements with match-count assertions; it could never execute and was deleted. Worked by: **`cp` via the Bash tool** for the file copy (permitted where `Copy-Item` was not), the **Read + Edit tools** for all 27 corrections (Edit's uniqueness check gave the same safety the script's assertions would have), and **PowerShell `@'…'@` here-string with `git -C`** for the commit. ⚠️ **`cd X && git …` worked in Bash this session while `git -C … add` was refused — the exact inverse of last session's finding.** Don't assume either form; try both.
- **Friction:** gen-fail — I wrote "26 edit operations" into the corrected transcript's header when the real count was **27** (25 rows, but rows 8 and 16 each cover two lines *and* rows 7 and 18 both land on L444). Caught only because the post-apply verification grep returned 28 matches against an expected 26. Worked by: **always reconcile the grep count against the row count before writing any total into a header** — the arithmetic on a/b-split rows is easy to get wrong twice in the same direction.
- **Next:** Unchanged in substance. See the DO NEXT block — resolve the duplicate, and note that **item 1's Supabase blocker has moved** (see below).
- **Watch out:** ⚡ **The Supabase blocker is no longer the allowlist typo.** `mcp__supabase__execute_sql` was **callable this run** — no permission prompt — and the **server** returned `Unauthorized. Please provide a valid access token… SUPABASE_ACCESS_TOKEN.` So the settings string now matches; the server is missing its token. `supabase-account2` and `supabase-cutter` are still un-allowlisted and raised prompts a non-interactive run can't answer. **Neither roll query ran; 0 rolls cross-referenced; the sync-gap check is still outstanding, not passed.** DO NEXT item 1 has been rewritten accordingly. ⚑ **`Hunter` is now well-evidenced as a first-run ASR hallucination** — it appears nowhere in this second run of the same audio; candidate retirement from `Names & Terms`, still Taylor's call, still unchanged. ⚑ **L20 `wonderful throw` (approved S11) vs `wonderful thrall` (this run) is the one place a second run actively disputes a word already in the approved S11 transcript** — worth a 10-second listen. ⚑ **`Pins and Needles` attribution is now disputed**: the two runs disagree on whether L1540 is SPEAKER B or G, and the S11 note attributes it to Zelda on the S11 diarization alone. ⚠️ `⟦FILL: player languages / accents⟧` and `⟦FILL: Vega voice guide⟧` both still unfilled.

### 2026-08-28 21:55 ET · Claude Code
- **Changed:** Ran Convo 1 **Phase A only** on `13-062226_raw_transcript.md` and stopped at the human-review gate — nothing applied, no notes, no Supabase, no `.docx`, no Convo 2. **The session identity does not check out and I did not resolve it:** the file is a **second AssemblyAI run of Session 11's audio** (identical source MP3 string, 137-min duration, model, 95.1% confidence, same first/last lines; only the transcribe timestamp differs — 2026-08-29 vs 2026-08-03), and S11 is already complete. Banked the transcript as evidence without renaming or deleting it, and wrote `_pipeline/S13/{spellcheck,flags,summary}.md` + `READY_FOR_REVIEW`. **29 proposed corrections, 9 at/under 60%, 28 flags.** 26 rows are the Taylor-approved S11 rows re-anchored to this file's line numbers with their S11 disposition (`applied`/`held`) recorded; **3 carry a standing "approved but do not apply" instruction and are reproduced as held.** ⚡ **The genuinely new finding is that the two runs disagree, and the diff is evidence:** diarization improved (SPEAKER B **131→81** turns, G **78→96**, partially fixing the S11 catch-all flag), `Beastrell C` → `Beast Thrall C` is fixed, `Beast Thrall F` **regressed** to `Beast Thrall Elf`, `wonderful throw` vs `wonderful thrall` diverged outright, and the unresolved glossary term **`Hunter` vanished entirely** — the same 00:19:40–00:19:51 window transcribes as ordinary combat resolution, which is good evidence it was a first-run ASR hallucination.
- **Commit:** `0802563`
- **Friction:** gen-fail — shell chaining blocked again, **sixth session running**. `cd X && …` was refused outright for git ("can execute untrusted hooks"), a `for`-loop over both transcripts died on `simple_expansion`, process substitution `<(…)` was rejected, `: > file` was refused as "multiple operations", a `Compare-Object` one-liner was blocked as "script block that may execute arbitrary code", and a PowerShell status command was blocked for `$()`. Worked by: `git -C <abs-path>` for every git call (avoids the `cd` refusal entirely — **use this form here from now on**), `Write` with empty content to create the marker file, and separate single-purpose calls for inspection.
- **Friction:** gen-fail — the commit here-string was rejected at **1258 bytes** ("max 965"). Worked by cutting the body roughly in half and pushing the detail into this entry. **Keep Ashfall commit messages under ~900 bytes.**
- **Friction:** misread — **PowerShell's working directory had silently drifted** to `Session_Sources/Transcripts/Raw_Unedited`, so `git status --short` reported the untracked file as a bare root-level path and briefly looked like a *different* file from the one in the session-start status. Caught it with `Get-Location`. Worked by switching to absolute `git -C`. ⚠️ **Do not trust the PowerShell CWD in this repo; the Bash and PowerShell tools do not share one.**
- **Next:** Resolve the duplicate — merge/replace the S11 raw transcript with this cleaner run or discard it — then find the real Session 13 recording (~070626). See the DO NEXT block.
- **Watch out:** 🛑 **Do not process `_pipeline/S13/` as a new session.** Applying those 29 rows to a "Session 13" would duplicate S11 canon and corrupt session keying; `flags.md` §6 lists the S11 worldbuilding that would be re-added. 🛑 **Any speaker map inherited from the S11 spell-check log will be wrong for parts of this file** — ~50 turns moved between letters, so script-format conversion stays blocked for both. ⚠️ **The `Hunter` glossary entry is now probably retirable**, but that's Taylor's call and nothing was changed in `Names & Terms`. ⚠️ **The watcher's first real end-to-end test produced a mislabeled duplicate** — plausibly it was re-pointed at an old recording and auto-numbered the output as the next session. Worth checking before the next session drops. ⚠️ `⟦FILL: player languages / accents⟧` still unfilled in both instruction files, **sixth session running** — every spell-check judgment was made on context alone.

### 2026-08-28 16:40 ET · Claude Code (watcher was never installed)
- **Changed:** 🛑 **This vault had no working automation at all.** The `Ashfall Pipeline Watcher` scheduled task had never been registered — no task, no process — even though every script it needs was present and complete (`ashfall_pipeline_watch.js`, `ashfall_transcribe/transcribe.js`, `Approve-Ashfall.cmd`, `Install-AshfallTask.cmd`). Registered it, patched `Install-AshfallTask.cmd`, and corrected `TRIGGERABLE_ACTIONS.md` §7, which still read *"Status: not registered."* Found during a three-vault audit of the campaign automation.
- **Commit:** `ca0357c`
- **Verification:** ✓ Task registered and read back: `Running`, `ExecutionTimeLimit=PT0S`, `RestartCount=3` at `PT1M`, both battery flags `False`, `MultipleInstances=IgnoreNew`. ✓ **Both** processes confirmed alive — `wscript.exe` 32944 + `node.exe` 35736 running `ashfall_pipeline_watch.js`. ✓ Principal matches the other two vaults exactly (`theli` / Interactive / Limited). ✓ Installer's embedded PowerShell syntax-checked with the PS parser (0 errors). Settings were copied from the healthy `SITL Pipeline Watcher` rather than invented.
- **Watch out:** 🛑 **Do not let the installer's settings block be removed.** `schtasks /Create` on its own inherits `ExecutionTimeLimit=PT72H`, `StopIfGoingOnBatteries=True`, `RestartCount=0`, which is fatal for a logon watcher meant to run forever — the sibling **WTFF watcher was found dead from exactly that** on the same day, killed at its 72h limit with its `node` child orphaned. ⚠️ **A watcher is only healthy when BOTH a `wscript.exe` and a `node.exe` exist for it**; task state alone lies. ⚠️ **This watcher has never actually processed a recording** — it is newly installed and unproven end to end. The first real Ashfall recording dropped into the watched folder is the true test. ⚠️ This vault is the stalest of the three (last content commit 2026-08-03) and is missing the `_Workflows Index.md` and onboarding note its siblings both have.
- **Next:** Unchanged. See the DO NEXT block above — fix the `mcp__plugin_supabase_supabase__execute_sql` allowlist string in `.claude/settings.local.json`.

### 2026-08-03 19:58 ET · Codex
- **Changed:** Replaced the condensed Session 12 recap DOCX with a **full-fidelity Word version of the complete canonical note**. It now carries the source metadata, POV Journal, Summary, every section/table/quote, Threads, Archivist Notes, and Related links from `Session 12 — Only the Relics Can End Their Rule.md`. Structural check: 293 paragraphs, 9 tables, all major canonical-section markers present; accessibility audit has 0 findings. Visual render QA remains unavailable because LibreOffice is missing in this runtime.
- **Commit:** `a4a6bdc`
- **Next:** Use the full Session 12 DOCX as the complete session reference.

### 2026-08-03 19:54 ET · Codex
- **Changed:** Added a casual, player-facing **Session 12** DOCX recap at `01-Sessions/Session 12 — Only the Relics Can End Their Rule.docx`. It distills the Dawnkeep attack, three-relic quest, shopping/downtime wins, and immediate hooks into a skimmable table-reference packet. Structural accessibility check passed without high-severity findings. Visual render QA could not run because LibreOffice is unavailable in this runtime.
- **Commit:** `71bc998`
- **Next:** Use the Session 12 recap for the immediate table reference.

### 2026-08-03 17:20 ET · Claude Code
- **Changed:** Ran **S11 Convo 2 — full vault propagation** for *A Delightful Chase* (06/22/2026), non-interactive, native FS (no Obsidian MCP). **7 files created:** the entire `S11-S20` tracker set (`Loot Tracker`, `Quote Board` 60 quotes, `Profanity Ledger` with running totals carried forward, `Roll Statistics`), plus **`The Dead Zone.md`** (the S10-flagged gap — a glossary Region that had no page despite being a whole session's setting), **`The Cloaked Figure.md`** and **`The Massive Thrall.md`**. **24+ updated:** Dashboard (S11 row, **threads 56–67 opened**, 7 moved to Resolved, NPC directory +5, timeline, **Party Snapshot rewritten**, tracker Reference rotated); Vega's journal (collapsible callout, verbatim); **all 7 PC pages** with site-parser `- **S11` Key Events bullets — Vega additionally +6 Inner Life entries, +6 quotes, +6 relationships, an Items block and a new **bear** aspect row; `Mr. Cat`, `Bobby`, `Dr. Wilson`, `The Beast Tamer`; **`Beast Thrall` substantially rewritten** with the DM's live statblock disclosures (~86 HP, the below-50 **"blood trait" frenzy**, **double damage to structures**, **radiant doubling**, the **eyed variant confirmed**); both library and castle location pages; `Setting Primer` (+3 sections, +9 open questions) and `Names & Terms` (`## S11 first-seen terms`, 15 entries, **none canonized**); `House Rules & Rulings` **+10 standing / +8 deferred**; `Homebrew Abilities` +3; `Spell_Usage` +29 rows; `DM Questions — Open` +5/−2; four pointer stubs rotated and the four `S01-S10` trackers marked 🔒 closed. **`Vault Sync Status.md` updated last** with the full S11 matrix and a change-log row. Also banked the untracked **S12 raw transcript** (`12-062926`).
- **Commit:** `5696807` *(plus `84dd76e`, which the Obsidian git plugin auto-committed mid-run — see friction)*
- **Friction:** gen-fail — Supabase `execute_sql` permission-denied for the **fifth consecutive run**. Worth recording precisely, because this pass **proved** the root cause rather than inferring it: the `supabase` MCP server *did* connect mid-session and exposed **`mcp__supabase__execute_sql`**; the permission prompt was raised against that exact live tool and denied, because `.claude/settings.local.json` allowlists `mcp__plugin_supabase_supabase__execute_sql` — a name matching no connected server. **It is a one-string fix.** I did not route around the gate. Consequence unchanged: S10 and S11 roll logs are both transcript-only and the sync-gap check is still outstanding.
- **Friction:** gen-fail — **the Obsidian git plugin's 10-minute auto-commit fired mid-run** and swept ~28 of my in-progress files into `84dd76e` under *its* message, splitting one logical unit of work across two commits with an unrelated subject line. Nothing was lost, but the S11 sync is not readable from a single commit. Worked by checking `git log` before committing rather than assuming a clean tree, then writing the remainder as `5696807`. **Long-running vault work in this repo will always race the auto-commit — check the log before you commit, and expect the split.**
- **Friction:** misread — I drafted two DM questions (Barrett's 18-vs-19 crit range, and "what is Pins and Needles?") that **violate this vault's own standing rule**, stated at the bottom of `DM Questions — Open.md`: homebrew wording is deliberately *not* on that list, because Taylor is a player and doesn't have the DM's homebrew cards. Caught it on reading `Homebrew Abilities.md`, which already had both filed as canon-in-play. Worked by removing both rows, renumbering, and softening the `Names & Terms` entry to point at `Homebrew Abilities` instead of demanding confirmation. **Read `Homebrew Abilities.md` before adding anything to the DM list.**
- **Friction:** gen-fail — shell chaining blocked *again*, fifth session running: `for`-loops and `cat` over multiple files died on `simple_expansion`, a PowerShell replacement was rejected for `$()`, and even the prescribed `git add -A && git commit -m … && git push` was refused as "multiple operations". Worked by dropping to `Read`/`Grep` for inspection and running the whole git motion as a **single PowerShell call** (`git add -A; git commit -m "…"; git push`), which passed. **The multi-`-m` commit form is also blocked here — one `-m`, and put the detail in the handoff.**
- **Next:** Fix the `settings.local.json` Supabase tool name, then run the roll queries interactively for both 2026-06-15 and 2026-06-22 and reconcile both `Roll Statistics` files. After that, S12 (`12-062926`) Convo 1 Phase A — but add the pipeline lockfile first.
- **Watch out:** ⚠️ **Roll Stats is the one S11 checklist row that is NOT ✅** — it's marked ⚠ transcript-only in the sync matrix, on purpose. Don't let a green-looking matrix hide it. ⚠️ **[[The Cloaked Figure]] is a placeholder and must not be merged** into [[The Beast Tamer]] or [[The Beast Master]] without the DM — the behavioural overlap is strong and entirely circumstantial, and the page says so. ⚠️ **SPEAKER B is a diarization catch-all** mapped to Deanna/Madi, so her profanity counts and several quote attributions are the least reliable data in the S11 trackers; it's flagged in place on every affected file. ⚠️ **Two standing house rules now contradict each other on grapple escapes** (S09 flat save vs S11 opposed check) — both are live in `House Rules & Rulings`, and it ran three times in S11. ⚠️ **No `Sun Shard` page exists** anywhere in the vault, so `[[Sun Shard]]` / `[[The Sun Shard]]` are broken links campaign-wide — pre-existing, not introduced here, but the relic now has a confirmed damage profile and nowhere to live.

> Older entries archived to handoff-archive/2026-08.md
