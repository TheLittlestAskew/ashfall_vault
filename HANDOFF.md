# HANDOFF — ashfall_vault

> Obsidian D&D vault for the "Ashfall Britannia" campaign (Tayls is a PLAYER, not the DM). Includes ddb-roll-sync and Site_HTML.
> Handoff is **enabled** for this repo. Every change updates the DO NEXT block below and prepends a log entry.
> Note: this is a notes/content vault — most session-note edits won't have a "next dev step." Use the DO NEXT block for things like next-session prep if useful, or leave it as "—".

## ▶ DO NEXT — 🛑 **Restore `SUPABASE_ACCESS_TOKEN` and run the two S18 roll queries**, then **start S18 Convo 2** from `_pipeline/S18/handoff.md`. ✅ **S18 CONVO 1 IS COMPLETE AND BANKED (`d0a9f7c`)** — corrections applied, corrected transcript + spell-check log + the canonical note `01-Sessions/Session 18 — I Knew You'd Come.md` all written. ⏸ Still queued behind it: **confirm the S18 speaker map** (`_pipeline/S18/flags.md` §1 — still the blocker on script-format conversion), **six DM questions**, and the long-standing **`Roll Statistics S11-S20` §S16 backfill** (empty stub since 09-03). ✅ **S17 IS FULLY SYNCED.**

> ✅ **S18 CONVO 1 (PHASE B) IS DONE (2026-09-15, `d0a9f7c`).** Title chosen from the play: **Session 18 — I Knew You'd Come**, Val's first words as the chains shatter, verbatim from the DM's closing boxed text — chosen because it is the **payoff line for Vega's S17 vow**. ⚑ **No title was named on the record** (unlike S16/S17), so this is the Archivist's pick and wants your yes. Alternates logged in the note. Banked: `Session_Sources/Transcripts/Corrected/18_091426_corrected.md`, `Session_Sources/Transcripts/Spell_Check_Logs/091426_Spell_Check_Log.md`, and the canonical 8-section note with the site parser contract validated (`## Metadata` + Location row · `## Summary` · `## Threads` · `## NPCs` · `## Archivist Notes`). Convo 2 handoff at `_pipeline/S18/handoff.md` (gitignored), first line = the note path. **No `.docx`.**
>
> ✅ **All 18 approved rows applied — 22 line-instances, grep-verified: 0 originals remain, all 22 replacements present.** Every edit was **line-anchored + exact-substring**, one occurrence per anchor line, so a drifted source would have failed loudly rather than mis-edited. ⚠️ **I applied rows 16–18 even though Phase A recommended holding them**, because you approved the table and the instruction was to treat it as final. **Row 18 (`Bite the Bullet` → `Risk Bullet`) is the one to look at** — Phase A's own read, which I agree with, is that **S18 was probably the CORRECT hearing**; the names sound nothing alike, so this is not ASR drift. The original readings for all three are preserved in the spell-check log, so **reverting row 18 is a one-line change**. ▶ **Ask Doug.**
>
> 🛑 **THE ROLL ARCHIVE WAS NEVER QUERIED — same failure as S16, and it is the top blocker.** `mcp__supabase__execute_sql` returned **`Unauthorized`** (`.mcp.json` interpolates `SUPABASE_ACCESS_TOKEN`, still unset), and **`supabase-account2` *and* `supabase-cutter` both hit an un-grantable permission prompt in the non-interactive run** — ⚠️ **note that `supabase-cutter` is the route TOOLS.md records as "the one that actually works"; it did not work headless.** Neither `SELECT * FROM ashfall_session_rolls WHERE session_date = '2026-09-14'` nor `SELECT MAX(timestamp_iso)` ran. ▶ **Consequences:** the note's **~110-row Full Roll Log is transcript-only and flagged unverified on every row**; **no sync-gap determination was possible** (an access failure is *not* a confirmed gap); and **nothing is marked `physical dice roll`**, because that label requires an archive to be absent from. **No rolls were fabricated.** ▶ **Run both queries before Convo 2 touches [[Roll Statistics S11-S20]].**
>
> 🛑 **The corrected transcript deliberately KEEPS the A–H diarizer labels.** Project Instructions Step 3 asks for character names; `flags.md` §1 says not to until the map is confirmed. **`flags.md` wins** — `SPEAKER F` merges Barrett + Val + the DM's own adjudications across 240 lines and `SPEAKER C` merges Zelda + Lulu, so converting would have injected ~100 false attributions (No Invention). The inferred map is in the transcript header, marked unconfirmed. ⚠️ **Every speaker attribution in the note's Quote Board and Profanity Record inherits that uncertainty**, and both say so.
>
> ⚠️ **Phase A was WRONG about `Boilborn` and the note corrects it.** It is **not** first-seen — it debuted in **S15** and has a full vault page at `07-Flora_Fauna/Creatures/Boilborn.md` (a DM-built *anti-tank* aimed specifically at Vega). The ≤60% first-seen cap on rows 13/14/16 rested on a false premise, so **those corrections are better-supported than the table claims**: S15's recorded **Rupture Burst — DC 16 Constitution** matches S18's *"The DC is 16"* and 34/17 damage exactly. ⚑ It is still missing from `Names & Terms.md`, but that is the **six-session glossary gap**, not a first sighting.
>
> ⭐ **Two things buried in the play that nobody flagged.** (1) 🛑 **THE SUN SHARD IS A SET OF THREE** — *"with the gauntlet and the sword, add an extra 2d8 fire damage. Because you have **2 of the set of 3**."* Said once, in passing, at 00:15:56. **The third piece has never been named** and this belongs on the Sun Shard page. (2) **Val has immortality to lose** — the ritual was siphoning *"his magic, his **immortality**, and his very essence"*, his skin *"mortal one moment, immortal the next."* First time that is on record.
>
> ⚠️ **Redaction is still OUTSTANDING for S18** and the raw transcript is already public (`abf9ab8`). The corrected transcript adds **no new exposure** — it is the same content — but it does double it. `flags.md` §11 has the list; the load-bearing one is 🛑 **the DM identifying a player as his wife** (L1930), which matters for the speaker map and must not reach a public artifact. **None of it reached the session note.** ▶ **Your call, same as S15–S17.**

> ✅ **S18 PHASE A IS DONE (2026-09-14, `abf9ab8`).** Raw transcript banked at `Session_Sources/Transcripts/Raw_Unedited/18-091426_raw_transcript.md`; review packet at `_pipeline/S18/` (gitignored): **`spellcheck.md` (18 proposed corrections, 7 at/under 60%), `flags.md` (14 sections), `summary.md`, `READY_FOR_REVIEW`.** Nothing applied, transcript untouched. ✅ **Session number and date confirmed, no discrepancy — S18 / 09/14/2026**, six-way cross-check (filename · `091426 Ashfall Britannia Recording.mp3` · *"7 PM"* called at 00:05:41 · transcription timestamp 11:28 PM ET the same night · **S17 exactly 7 days prior** · Val's rescue picking up S17's cliffhanger, incl. the S17 flower crowns and *"It's been 3 weeks"*). **2,502 lines, 18,059 words, 165 min, 95.7%, 8 speakers, zero `[inaudible]` markers, zero encoding artifacts.** Full table, no absences — **Chase attended with no mechanical turns** (Val is the captive; the DM rules *"Val can scream in pain on his turns"*), then becomes the most-quoted speaker once rescued. **Party level is 13**, up from the vault's 12 for S17.
>
> 🛑 **THE S18 SPEAKER MAP IS THE BLOCKER AND WAS DELIBERATELY NOT GUESSED.** **`SPEAKER F` is a 240-line catch-all spanning at least three people** — Barrett (Doug), Val (Chase) **and the DM's own attack adjudications** — and **`SPEAKER C` merges Zelda with Lulu**. Firm from evidence: **A = the DM** (393 turns, all narration and monster turns) · **B = Josh / Samothy** (runs Mr. Cat; the DM addresses him as Josh at L280) · **D = Taylor / Vega** · **E = Jill / Flux** (Witch Bolt, Sneak Attack, the DM's *"I forgot you were a rogue"*). **G and H are filler only and unassignable.** ⚠️ **Do not carry S17's map over** — S17 had `E` = the DM; here `E` is Flux and `A` is the DM. ▶ **One question at the table, or an audio listen**, before any script-format conversion hard-codes names.
>
> 🛑 **Six DM questions came out of S18**, in priority order: (1) **who is "The Beastmaster"** in the opening boxed text at L226 — the *only* use in the session against ~30 uses of *"the necromancer"*, and **[[The Beast Master]] died in S10**; (2) **`Boilborn` — is that the spelling?** New enemy type, three ASR renderings (`Boilborn` ×6 · `Oilborn` · `Wildborn`), capped at 60% by the first-seen rule even though his own description (*"all these boils just started swelling… then they burst open"*) makes the intent plain; (3) **`Arona`** — Val's new power source, ⚑ possibly S16's **`Book of Varona`**, **not assumed**; (4) **`Lunara, the Nightbringer`** — new deity, the ritual crystal is *"a replica of the Sun Shard… but the darker version"*; counterpart to **Emberos**?; (5) **is the tower 110 or 120 feet?** — he says 110 twice, then lets 120 stand twice uncorrected, and half the session's distance rulings hang on it; (6) **Deanna's reincarnation window** — Lulu states 10 days, but in-world elapsed time may already exceed it, and the table deferred the whole decision to S19.
>
> ⚡ **One row I recommend REJECTING outright:** row 18, `Bite the Bullet` → `Risk Bullet` (40%). **S18 is probably the correct hearing** — the two names sound nothing alike, so this is not ASR drift, and the S17 `Risk Bullet` entry is provisional ⚑. The row exists only to surface the cross-session conflict. **Ask Doug.**
>
> ⚑ **No session title was named on the record** — unlike S16 and S17, the table did not christen this one. Convo 1 Step 6 will need the five-alternate proposal route.
>
> ⏳ **Open threads carried into S19:** the **8 enchantable crystal shards** (DM deferred their utility on the record — *"I didn't think anybody would ask… I'll get to that probably tomorrow"*) · a promised **custom 12d12 spell** that was never handed over on the record, recipient unknown · the **Deanna reincarnation conversation**, explicitly deferred (*"Not tonight"*) · the **Soulbound Grimoire's** holder, unassigned.
>
> ⚠️ **Privacy: the S18 raw transcript was banked UNREDACTED.** Same exposure class as S16/S17 and it is now public. `flags.md` §11 lists what is in it: household names (`Kenny`, plus the dogs `Eldon`/`Asta`/`Hobo`/`Caramel`), *"we gotta get the kids down"*, a **real-world murder reference aimed at the DM** (L1936), explicit OOC at L794/L1326, a drug reference at L80, and 🛑 **the DM identifying a player as his wife** (L1930, *"that is my wife that I killed for my first player kill"*) — which is load-bearing for the speaker map **and** should not reach any public artifact. **No redaction pass was run**, because the standing Tier-0 policy is still unruled (item 4 below). ▶ **Your call, same as S15–S17.**
>
> ✅ **S17 PHASE A IS DONE (2026-09-09).** The 09-07 OAuth stall is cleared — Phase A re-ran clean from the watcher's own prompt file, no re-transcription. Review packet is at `_pipeline/S17/` (gitignored): **`spellcheck.md` (26 proposed corrections, 13 at/under 60%), `flags.md` (15 sections), `summary.md`, `READY_FOR_REVIEW`.** Nothing was applied; the raw transcript is untouched. ✅ **Session number and date confirmed with no discrepancy — S17 / 09/07/2026**, four-way cross-check (filename · `090726 Ashfall Britannia Recording.mp3` · *"It is 8:02 PM"* at 00:00:09 · transcription timestamp landing 3 min after the 176-min audio ends). Longest Ashfall transcript to date: **2,922 lines, 21,839 words, 8 speakers, zero `[inaudible]` markers.** Full table, no absences; Chase played the doppelganger all session and it **dies at the end**, against the DM's stated plan of three more sessions.
>
> ✅ **THE RAW TRANSCRIPT IS REDACTED AND BANKED (2026-09-14, `76d504e`) — Taylor chose option A.** `Session_Sources/Transcripts/Raw_Unedited/17-090726_raw_transcript.md` now carries **97 markers: 96 `[personal conversation - redacted]` + 1 `[slur - redacted]`**, against S16's 24 — S17 genuinely held more. Unredacted original preserved at **`_pipeline/S17/17-090726_raw_transcript_UNREDACTED.md` (gitignored, md5-verified before the pass)**, so **every redaction reverses**. What went: the pre-session personal block (00:01:21–00:05:42 personal biography + an ethnic-joke exchange naming a non-participant · 00:06:25–00:09:06 spouse + autism self-disclosure · 00:16:52–00:20:06 heights and dating history, which identified an ex-sister-in-law and nephews as little people and named a third party's daughter), food orders and the **prescriptions/cannabis** exchange at 00:52:57–00:55:00 (both named a non-participant present in the room), the *"kill your wife a second time"* line from `flags.md` §10, and the **r-slur at 02:17:20**. ⚠️ **No campaign content was touched** — blocks start and end where table talk resumes being about the game; *"Call it the Undercroft"* (L78, a title candidate) and the L10 *"It is 8:02 PM"* session-dating evidence both survive, and the slur line **keeps its mechanical result** (*"So he just misses both his attacks [slur - redacted]"*) for the roll log.
>
> 🛑 **The redaction was line-preserving ON PURPOSE, and that constraint is permanent for this vault.** `spellcheck.md` cites **bare raw-line numbers** (524, 548, 1070, 1746, 1890 …), so deleting or merging a single line silently breaks every citation in the review packet. The pass replaced spoken text **in place** and left all 2,923 lines and every `[HH:MM:SS] SPEAKER X:` prefix intact; all 13 cited lines were re-checked after the write and still resolve. ⚠️ **Write these files LF-only** — Python's `write_text` on Windows silently converts to CRLF, which made all 2,922 lines show as changed and buried the 97 real edits until it was caught. S16 and the source are both LF.
>
> ⚑ **The standing gitignore recommendation is now MOOT for S17 but still live as policy** — see item 4 below. Redacting per-session works, but it is a judgement call every time; **18 older transcripts are still public and still unaudited.**
>
> ✅ **BATCH A+B IS APPLIED AND BANKED (2026-09-14, `31279ec`) — the corrected transcript exists.** `Session_Sources/Transcripts/Corrected/17_090726_corrected.md` (**15 rows · 24 line-instances · 20 distinct lines**) plus a full `Session_Sources/Transcripts/Spell_Check_Logs/090726_Spell_Check_Log.md`. Every edit was an **exact-string replace with an expected occurrence count**, so a drifted source would have failed loudly instead of mis-editing; all 24 verified present afterwards and all 12 old forms verified gone. 🛑 **Row 15 was REVERSED, not applied, and asserted as an invariant** — `Theros Emberborn` is still on raw line 1622 and `Tharos` is still at 4 occurrences. The `[slur - redacted]` marker was likewise asserted to survive Phase B.
>
> ⚡ **Row 21 is still yours and it is the DO NEXT:** `Tails` → **`Tayls`**, raw L622, Chase asking *"Tails, what weapons does Vega have on her right now?"* — he is addressing **you**. **95%.** Confirm the spelling and it applies in one pass, along with promoting the provisional S08 glossary entry to confirmed.
>
> ⚑ **THE S17 SPEAKER MAP IS UNRESOLVED AND WAS DELIBERATELY LEFT BLANK.** 🛑 **Do not carry S16's map over — it does not match:** S16 had `E = Taylor / Vega`, and in S17 **`E` is unambiguously the DM.** Firm from evidence in the transcript: **E = the DM** (373 turns, sets every scene) · **B = Chase / the doppelganger** (addresses you by name at 622, shapechanges into Deanna at 416) · **C = you / Vega** (*"who am I? Vega is still raging"* at 266; answers the str check + dex save asked of Vega at 728→730) · **D = Barrett's player** (narrates Barrett third-person 4×, incl. 1354). **A, F, G, H are NOT resolved** — `F` says *"I'm a druid"* (1170), which fits Lulu, but also refers to Lulu in the third person 5×; `G` speaks row 4's line that Phase A read as *"Samothy's own player"*, yet `G` names Mr. Cat 4× and Samothy once. ▶ **One question at the table, or an audio listen.** A wrong map propagates into every downstream note, so it was not guessed.
>
> ✅ **All 26 rows were reviewed 2026-09-12**, each verified against the raw transcript: **15 apply · 4 reject · 5 hold for audio · 1 reversed · 1 Taylor's.** ⚠️ **`spellcheck.md` itself was never rewritten** — it still reads *"NOTHING HAS BEEN APPLIED"* at the top, which is now false. The authoritative record of what was applied is the **spell-check log**, not the proposal set.
> - ▶ **Batch A (apply as-is):** rows **1, 2, 3, 6** — glossary-confirmed or ordinary English.
> - ▶ **Batch B (verified, confidence raised, apply):** rows **4, 5, 7, 8, 9, 10, 11, 12, 13, 16, 17**. Biggest movers: **5** `For dicks`→`For Dex` 85→**92%** (the DM asks for *"a strength check and then a dex save"* at raw L728); **6** `Traips`→`Traipse` 85→**95%**; **7** `Prince, repeat`→`rinse, repeat` 80→**90%**; **8** `Eric`→`Barrett` 75→**85%** (3rd→1st person switch on his own PC, gunpowder = the Gunslinger); **13** `imposter`→`impostor` 65→**85%**; **17** `Emily`→`Sammy` 55→**80%**.
> - 🛑 **The ≤60% proper-noun cap is miscalibrated for rows 16 and 17** and that is worth fixing in `Convo_1_Instructions.md` before S18. The cap exists to stop *canonizing* first-seen proper nouns — but **17 removes a spurious one** (`Emily` has no referent anywhere; the refrain is *"Samothy, please"* ×3 in the same scene) and **16 is capitalization only** (`gauntlets of burning grasp`→`Gauntlets of Burning Grasp`, the dungeon objective). Neither invents canon, so neither should have been capped.
> - ✗ **Reject — the Verbatim Quotes rule protects these:** **14** `death save`→`Dex save` (he self-corrects *in the same line*: *"so it's a dex 17"* — editing it erases a real moment); **23** `major, major restoration`→`Greater Restoration` (a **DM** mechanics statement, "No DM Override" applies; *"need a really good cleric"* does support *Greater*, fwiw); **22** `one-show pony`→`one-trick pony` (Zelda is a **Bard** — a show pun is in character); **26** `Demonically what Sammy would do` (the 30% row, reads fine as casual speech — **recommend dropping the row entirely**).
> - ⏸ **Hold for an audio listen:** **24** `Strangecage`→`Bone Cage` (semantically right, phonetically poor — this one really needs ears), **25** `Paching`→`Ka-ching`, **18** `Whisperwire`, **19** `bust`→`bus` (50→60%), **20** `Jifusit`→`GIF you sent` (50→65%).
> - ⚡ **Row 21 is yours to close in five seconds:** `Tails`→**`Tayls`**. Raw L622, Chase asking *"Tails, what weapons does Vega have on her right now?"* — that is him addressing **you**. Third hearing, first from a new speaker → **95%**.
>
> 🛑 **ROW 15 SHOULD BE REVERSED, NOT APPLIED — and DM question #1 may now be answerable.** The table proposes `Theros`→`Tharos`; the evidence points the other way. The three spellings are **not equal-weight**, because of what he was doing each time: **L1348 `Tharos`** (ad-lib, mid rules explanation) · **L1618 `Tharos`** (ad-lib — *"this is the tomb of Tharos Emberborn. **I don't know where it is**"*, he is hunting for his note) · **L1622 `Theros`** (*"**Sorry, I was looking at the wrong note**"* → then reads the prepared boxed text: *"**Theros** Emberborn lies inside, perfectly preserved…"*). ⚠️ **Inference, not fact, and one prepared block is thin evidence** — but his *written note* appears to say **Theros**, which matches S12 and makes S16's `Tharos` the verbal slip. The standing rule against retro-editing until he rules still holds; just **ask him as *"your notes say Theros, right?"*** rather than as an open coin-flip.
>
> 🛑 **Four DM questions came out of this session**, in priority order: (1) **`Tharos` vs `Theros` Emberborn** — now blocking S12, S16 **and** S17; ask it the narrowed way above; (2) the **exact title wording** — the table named it *"Hobo, That's Your Reflection"* on the record at 02:44:18 after he broke off mid-sentence to yell at his dog, but his originating line is transcribed two different ways; (3) is **Vega's now-cursed sword the Sunfire blade** from the S16 murals / S14's *"Blade of Sunfire"* (three references, still unreconciled); (4) **name the monks and their location** — he deferred it on the record at 01:33:03 and they are the only way to uncurse the blade.
>
> ⚠️ **Still open, one question at the table:** **Barrett cites "hitting level 11"** (raw L2404) against the vault's party level **12** — probably benign.
>
> ✅ **`⟦FILL: player languages / accents⟧` IS CLOSED (2026-09-12, `77a306d`), after eight sessions carrying it.** No accented or non-native English speakers at the Ashfall table; the accented players are all in *Sky Is The Limit*. Closed in all five live files including the Phase A prompt template, so **S18 onward inherits it.** Detail in the log entry below. ⚠️ Dated run records and historical spell-check logs were **deliberately not retro-edited**.

> ✅ **S16 CONVO 2 IS DONE (2026-09-03, `616a8df`).** Full propagation — **32 files, 6 created, 26 modified.** Everything on the completion checklist is ✅ **except the two items below, both of which are blocked on a permission, not on missing work.** Detail in [[Vault Sync Status]] under the S16 block.
>
> ⚠️ **BLOCKER 1 — HALF CLEARED, the run is still owed.** `mcp__supabase__execute_sql` **is now allowlisted** in `.claude/settings.local.json`, so the permission half of this is done; `.env` still holds only `DDB_COBALT`, so the `SUPABASE_ACCESS_TOKEN` route remains unfixed. ▶ **Still to do:** run `SELECT * FROM ashfall_session_rolls WHERE session_date = '2026-09-03';` and `SELECT MAX(timestamp_iso) FROM ashfall_session_rolls;` and fill [[Roll Statistics S11-S20]] §S16, which is deliberately an **empty blocker stub** — **no per-character stats were derived from the transcript, on purpose.**
>
> ✅ **BLOCKER 2 IS CLEARED — S16 IS PUBLISHED.** `Public Session Index.json` was regenerated and pushed in `643ebf0` on 2026-09-04; `all_sessions` now holds 16 entries ending at S16 and `sessions` holds 6, with S16 titled *What the Fuck* (2026-09-03). Verified 2026-09-09. Nothing further needed here.
> 🛑 **THREE FOUR-SESSION VAULT GAPS were found during the S16 run and flagged in place, not filled** (filling them is a copy job, not a rewrite — the source is in the S13–S15 session notes): **[[Names & Terms]]**, **`05-Mechanics/Spell_Usage.md`** and **[[Vega Bloodroot Journal]]** all jump **S11 → S16**. ⚠️ The journal gap is the one that stings — **S14 is Deanna's death and S15 is the relic**, the two biggest beats of the run.
>
> 🛑 **Still yours to rule on, all three carried from Convo 1 and now baked into the vault as provisional:** the **doppelganger's page name** (held at [[The Doppelganger]], **deliberately not merged** into Valerian's page) · the **Emberborn spelling** (S12 says `Theros`, S16 says `Tharos`, **neither canonized, neither transcript retro-edited**) · and the **privacy / r-slur policy**, still unruled across two sessions.

> ✅ **S13–S15 ARE FULLY PROPAGATED AND PUBLISHING (2026-08-30, `4313b16`).** Convo 2 is done for all three. The site index went from **2 published sessions to 5**, and `all_sessions` from 12 to 15.
> ✅ **The website blocker is cleared.** `generate_public_session_index.mjs` reads `00-Campaign-Hub/Campaign Dashboard.md`, **not** `01-Sessions/` — its table stopped at S11, which is why nothing since June could publish. Rows for 13/14/15 are in, along with the **Quote Board / Loot Tracker / Profanity Ledger** blocks the generator requires for any session ≥10. ⚠️ **Sessions ≥10 need BOTH a Dashboard row AND all three tracker blocks, or the generator throws and writes nothing at all.**
> ✅ **The three-session roll-archive debt is CLEARED.** S13, S14 and S15 all queried via `mcp__supabase-cutter__execute_sql` on project `vtrtyagltwdrbastpppl`. ⚠️ **Two gotchas, both recorded in [[Roll Statistics S11-S20]]:** `individual_values` is a **JSON string** (`"[6]"`) not an array — parse with `(individual_values #>> '{}')::jsonb -> 0` or every session reports zero nat-20s; and the archive **undercounts d20s** because several players roll physical dice, so treat its counts as a floor.

**▶ Next actions, in order:**

0. ✅ **S16 CONVO 1 *AND* CONVO 2 ARE BOTH COMPLETE** (`4ccfc37`, then `616a8df`). *The Convo 1 detail below is retained for the record; its three open rulings are still open and are restated in the block above.* — **S16 CONVO 1 IS COMPLETE AND COMMITTED (`4ccfc37`).** Title chosen from the record: **Session 16 — What the Fuck**. Note at `01-Sessions/Session 16 — What the Fuck.md` (all 8 sections, website parser contract validated); corrected transcript and spell-check log both banked. 15 of 19 approved corrections applied (20 line-instances); **4 held** per the approved table's own *"flagged not fixed"* heading — **say the word if you meant all 19 and it re-runs in a minute.** Convo 2 handoff is at `_pipeline/S16/handoff.md` (gitignored), first line = the note path. **Three things still need *your* call:**
   - 🛑 **THE ROLL ARCHIVE WAS NEVER QUERIED — this is the top blocker.** The `supabase` MCP returned `Unauthorized`; `.mcp.json` expects `SUPABASE_ACCESS_TOKEN` and it is **not set** (the vault `.env` holds only `DDB_COBALT`). `supabase-account2` / `supabase-cutter` point at other projects and weren't permission-granted. **This is an access failure, NOT a confirmed sync gap** — whether your post-session sync ran is *unknown*. **No rolls were fabricated**; the ~45-row roll log is transcript-only and flagged provisional on every row. ▶ **Restore the token, run `SELECT * FROM ashfall_session_rolls WHERE session_date = '2026-09-03'` and `SELECT MAX(timestamp_iso) FROM ashfall_session_rolls`, then amend the note's Full Roll Log and [[Roll Statistics S11-S20]] before Convo 2 touches anything.**
   - 🛑 **[[Valerian Hellebore]] has been a doppelganger for two sessions.** Confirmed on the record (Hunter's Sense: *"He's a changeling. Genetically altered changeling."*), and Chase confirms above-table it started when Deanna died. **Every in-character "Val" line in S15 and S16 belongs to the impostor**, including the S15 bullying of Lulu. **I did not retro-edit the S15 note and did not substitute character names into the corrected transcript** — a documented speaker map went in the header instead. The note links him as **`[[The Doppelganger]]`, a provisional placeholder**. ▶ **Rule on the page name** (separate NPC page / `Val (doppelganger)` alias / disambiguation block on Valerian's page). **Do not merge him into Valerian's page.**
   - 🛑 **The Emberborn hero's name has FOUR renderings across two sessions, and one is already in the vault.** `Theros Emberborn` was applied at 60% in the **S12** spell-check and stands in the corrected S12 transcript; S16 gives `Thalos`/`Tharos`/`Tharos'`, and `Tharos` was applied as the S16 majority hearing. **Neither is canonized.** ▶ **DM question. Do not retro-edit either transcript until he rules.**
1. 🛑 **Convert `01-Sessions/Session 12 — Only the Relics Can End Their Rule.docx` to markdown.** This is now **the single remaining blocker to a complete public record** — the index jumps S11 → S13 and the Dashboard has a visible gap. Use `Workflows/scripts/docx_to_text.py` (run `--selftest` first, it is **still unverified**). ⚠️ Convert the **approved `.docx`** — do *not* regenerate from `13_062926_corrected.md`, which would create a second divergent account. Then it needs a Dashboard row **and** S12 blocks in all three required trackers.
2. 🛑 **Three DM answers are blocking, and two of them block note amendments.** All are written up in [[DM Questions — Open]] under *S13–S15*:
   - **`Rite of the Dawning Radiance`** — S13 says [[Flux]] cast it as a ritual granting **Sunward Blessing**; S14 establishes it is **5th level** and she isn't taking it. **S13 note deliberately unamended.**
   - **[[Eiluned Lulu Denulie|Lulu]]'s subclass** — S14 records **Circle of the Land** in-fiction, S15 plays **Wildfire Druid**. **S14 note deliberately unamended.** Suggested fix: append *"— retconned in S15"* rather than rewriting.
   - **Do purity filters work on a corpse?** Deferred in S14, never answered. **It gates whether [[Deanna Smith-Wesson]] can be raised at all.**
3. ⚠️ **Check the Discord loot tab and reconcile two things** the DM promised in-session and never delivered aloud: the **obsidian tablet's contents** (S15) and the **gem values** for the 20 raw gems. The tracker records the DM's spoken list; **the tab is authoritative.**
4. 🛑 **PRIVACY — S16 is REDACTED and committed; the Tier-0 policy is STILL unruled.** Banking S16 meant publishing both transcripts, which is irreversible, so this pass followed the **established S15 pattern**: **24 `[personal conversation - redacted]` / `[slur - redacted]` markers in each** of `16-090326_raw_transcript.md` and `16_090326_corrected.md`, with the **unredacted original preserved at `_pipeline/S16/16-090326_raw_transcript_UNREDACTED.md` (gitignored)**. **No campaign content was touched; every redaction reverses from that file.** 🛑 **That is a hold, not your decision.** The **r-slur recurred in S16 (raw L1208), second speaker, second session**, and **the S15 instance remains unredacted and unruled** — whether to redact, quote, or handle it otherwise is still yours to call, across both sessions. **Standing recommendation, unchanged and now four-for-four: gitignore `Session_Sources/Transcripts/Raw_Unedited/` the way `Recordings/` already is.** ⚠️ **18 transcripts are now public and have NOT been audited.**
4b. 🛑 **`04-World-Lore/Names & Terms.md` ends at S11** — no S12/S13/S14/S15 section exists, four sessions after those notes shipped. Concrete cost, visible in `_pipeline/S16/spellcheck.md`: **`Emberborn`, `Eiluned` and `Depth Charge` all had to be capped at 60%** despite being used confidently across the vault, because the confidence rule keys on that file. **Run a glossary catch-up pass (S12–S15) before adding S16's terms**, so the cap goes back to meaning "genuinely uncertain."
5. ⚠️ **Website (`rectrixcaedere`) — confirm the live site picked S13–S15 up.** It fetches `Public Session Index.json` from this repo's `main` at request time, so the push should be enough (~5 min CDN). ⚠️ Ashfall's `sessionRollsOnly` stays **OFF** in `app.js` until the **S01/S02/S04 index-date off-by-one** is fixed — enabling it now would silently drop ~1,037 real rolls. ⚑ **S13 has the same off-by-one** (rolls keyed 08-03 for an 08-02 session); S14 and S15 are correct.
6. ⚑ **Ask the DM: who is the cloaked telepath?** (carried from S11, still open.) *"you led me on a delightful chase… I can't wait till next time."*
7. 🛑 **Add a lock to `Workflows/scripts/ashfall_pipeline_watch.js`.** A duplicate concurrent run fired during S11 Phase B and wrote two artifacts under the primary run.
8. ⚑ **Vault conventions worth a decision:** there is **no Items folder** ([[Vault Format Reference]] defines PCs/NPCs, Locations and Creatures only), so relics live in the Loot Tracker and `[[Blade of Sunfire]]` / `[[The Sun Shard]]` are de-linked or dangling. Relics are accumulating — the Sun Shard, the Blade of Sunfire, and now Vega's unnamed cursed longsword. ⚑ Also: **`[[Session 12]]` has no markdown target**, so it is written as plain bold everywhere.

⚠️ `_pipeline/` is gitignored — review packets, unredacted transcripts and Convo 2 handoffs live only on this machine.

---

### Standing work order (still pending, from the 2026-07-04 vault audit)
1. ⚡ **Check the recording app** (standing flag from Session 09: only Taylor's mic captured, no Discord audio). Test before Monday.
2. **Claude Code migration (from 2026-07-04 audit):** unify 10 session notes to the adopted superset schema — add `type: session`; rename `session:` → `session_number:` and `date:` → `session_date:`; kebab keys → snake (`party-level` → `party_level`, `roll-archive-date` → `roll_archive_date`). Ashfall notes carry no `type:` today, so this vault's DnD.base Sessions view currently matches ZERO notes.
3. **Publish command (new):** `Workflows/scripts/Publish-Ashfall.cmd` commits + pushes all note changes; the site fetches session notes straight from this repo's main, so push = publish (~5 min CDN).
4. **Inside Obsidian (never shell):** create `Templates/` and move the six root template stubs into it (they show as phantom blank rows in DnD.base); create `06-Media/` and move the two battle-map PNGs at root into it; move the `ddb-roll-sync/` extension folder under `Workflows/` to match SITL/WTFF layout.
5. **Parity items:** add `Workflows/_Workflows Index.md` and an `Onboard Ashfall Britannia.md` (mirror the SITL/WTFF versions).

---

## Log
<!-- newest first · one entry per logical task/session · timestamp · source · changed · commit · next -->

### 2026-09-15 00:20 ET · Claude Code (S18 CONVO 1 PHASE B — corrections applied + canonical session note)
- **Changed:** Applied all **18 approved spell-check rows / 22 line-instances** to the S18 raw transcript and banked three artifacts: `Session_Sources/Transcripts/Corrected/18_091426_corrected.md`, `Session_Sources/Transcripts/Spell_Check_Logs/091426_Spell_Check_Log.md`, and the canonical 8-section note **`01-Sessions/Session 18 — I Knew You'd Come.md`** (site parser contract validated: `## Metadata` with a Location row, `## Summary`, `## Threads`, `## NPCs`, `## Archivist Notes`). Convo 2 handoff written to `_pipeline/S18/handoff.md` (gitignored), first line = the note path. No `.docx`. Every edit was **line-anchored + exact-substring, one occurrence per anchor line**; post-apply grep confirms **0 originals remain and all 22 replacements present**. **Title chosen from the play** — *"I knew you'd come"*, Val's first words as the chains shatter, the payoff line for Vega's S17 vow; ⚑ **no title was named on the record**, so it needs Taylor's yes. Five alternates + five rejected candidates logged in the note.
- **Commit:** `d0a9f7c`
- **Friction:** gen-fail — the correction pass was written as a Python script (`_pipeline/S18/apply_corrections.py`) and **the script could never be executed**: a Bash heredoc died on `Parser skipped input between top-level statements`, then `python …`, `PowerShell Copy-Item` and `git commit -F` were each **permission-gated in the non-interactive run**. What finally worked: **`cp` via Bash to create the file, then 22 individual `Edit` tool calls** (one per correction, all verified by a single follow-up `Grep`), and **`git commit -F <file>` via the PowerShell tool rather than Bash** — the Bash form is gated, the PowerShell form is not. The dead script was deleted rather than left in the tree. ▶ **For headless vault runs: do not plan on running a script. Plan on `Write` + `Edit`, and commit through PowerShell.**
- **Friction:** gen-fail — one `Edit` failed with *String to replace not found* because I anchored on a timestamp (`[00:57:30] SPEAKER E: Wildborn.`) read back slightly wrong. Fixed immediately by dropping the timestamp and anchoring on `SPEAKER E: Wildborn.` alone. ▶ **Anchor edits on the distinctive text, not on the timestamp prefix.**
- **Next:** Restore `SUPABASE_ACCESS_TOKEN`, run `SELECT * FROM ashfall_session_rolls WHERE session_date = '2026-09-14'` and `SELECT MAX(timestamp_iso) FROM ashfall_session_rolls`, reconcile the note's Full Roll Log, then start Convo 2 from `_pipeline/S18/handoff.md`.
- **Watch out:** 🛑 **The roll archive was never reached** — `mcp__supabase__execute_sql` returned `Unauthorized`, and **both `supabase-account2` and `supabase-cutter` hit an un-grantable permission prompt**, including the server TOOLS.md records as *"the route that actually works"*. The ~110-row Full Roll Log is **transcript-only, flagged unverified on every row**; **no sync-gap determination was possible**; **nothing is marked `physical dice roll`** (that label needs an archive to be absent from). **No rolls fabricated.** 🛑 **Rows 16–18 were applied despite Phase A advising a hold** — you approved the table and I treated it as final; **row 18 (`Bite the Bullet` → `Risk Bullet`) is probably backwards** and reverts in one line from the spell-check log. 🛑 **The corrected transcript keeps the A–H diarizer labels on purpose** — converting to character names would have injected ~100 false attributions, so Quote Board and Profanity Record attributions are provisional. ⚠️ **Phase A was wrong that `Boilborn` is first-seen** — it debuted S15 with a full creature page whose DC-16 Rupture Burst matches S18 exactly, so rows 13/14/16 are better-supported than the table says. ⭐ **Two under-noticed canon facts now in the note:** the **Sun Shard is 1 of a set of 3** (third piece never named) and **Val had immortality to lose**. ⚠️ **S18 redaction is still outstanding** and the corrected transcript doubles the already-public exposure.

### 2026-09-14 23:55 ET · Claude Code (S18 CONVO 1 PHASE A — spell-check only, stopped for review)
- **Changed:** Banked the S18 raw transcript (**2,502 lines, 18,059 words, 165 min, 95.7%, `universal-3-5-pro`, 8 speakers, zero `[inaudible]`, zero encoding artifacts**) and wrote the Phase A review packet to `_pipeline/S18/` (gitignored): **`spellcheck.md` (18 proposals / 23 line-instances, 7 at/under 60%, plus a checked-and-left-alone list), `flags.md` (14 sections), `summary.md`, `READY_FOR_REVIEW`.** **Nothing applied** — no corrected transcript, no spell-check log, no Supabase query, no glossary edits, no session note, no Convo 2 work, nothing written outside `_pipeline/S18/`. ✅ **Date confirmed six ways, no discrepancy (S18 / 09/14/2026).** Biggest catches: **`66` → `6d6`** on Vega's cursed sword (85%, four instances — the statblock reads correctly at L408, which is the literal answer to *"how do you have 66?"*), **`for rating` → `for radiant`** (80% — Flux's party-wide +1d6 radiant is the session's whole damage math), and the **`Boilborn`/`Oilborn`/`Wildborn`** three-way split.
- **Commit:** `abf9ab8`
- **Friction:** `gen-fail` — **four separate shell-idiom rejections cost round-trips**, all in the same non-interactive pattern already logged against S17: Bash refused `: > READY_FOR_REVIEW` (output redirection), `cd "<path>" && git …` (directory change before git), a `python -c` one-liner with embedded quotes, and a `;`-chained `git -C` pair; PowerShell then refused the fallback for containing `$()`. What works, and it is now three sessions of the same lesson: **use the `Write` tool for file creation** (including empty marker files — `Write` with empty content beats any shell redirect) and **one `git -C "<path>"` invocation per PowerShell call**, with multi-line commit messages in a single-quoted here-string.
- **Friction:** `gen-fail` — **I corrupted the `TOOLS.md` table with my own edit and caught it one step later.** The `Ashfall Pipeline Watcher` row's `old_string` ended at `| Free |`, so the replacement text was inserted *ahead of* the existing Notes cell instead of replacing it, silently creating an **8th column** in a 7-column table that `septentrion-sync` parses positionally. What works: when bumping a `TOOLS.md` row, **match the whole row through its trailing `|`**, never just the prefix up to the column you're changing — and re-read the row afterward to count columns, because markdown renders a ragged table without complaining.
- **Next:** Review `_pipeline/S18/spellcheck.md` row by row, then confirm the S18 speaker map.
- **Watch out:** 🛑 **`SPEAKER F` is a 240-line catch-all spanning Barrett, Val AND the DM's own attack adjudications**, and `SPEAKER C` merges Zelda with Lulu — **the map was deliberately not guessed**, and S17's does not transfer (`E` was the DM there; here `E` is Flux and `A` is the DM). ⚑ **The opening boxed text says *"The Beastmaster"*** — the only use in the session against ~30 uses of *"the necromancer"*, and [[The Beast Master]] died in S10; **row 10 proposes the two-word spelling only and does NOT resolve the referent.** ⚡ **Recommend rejecting row 18** (`Bite the Bullet` → `Risk Bullet`) — S18 is probably the correct hearing; the row exists to surface the S17 conflict, not to fix it. ⚠️ **The transcript was banked UNREDACTED** and includes the DM naming a player as his wife (L1930) plus a real-world murder reference aimed at him (L1936) — Tier-0 policy still unruled. ⚠️ **Party level jumped 12 → 13** off-session, and S17's level-11-vs-12 flag is still open; settle both before the roll tracker is keyed. ⚑ **No title was named on the record** — S18 needs the five-alternate route.

### 2026-09-14 21:40 ET · Claude Code (S17 CONVO 2 — full vault propagation, every checklist row green)
- **Changed:** Full S17 propagation, **0 created / 27 modified**. Campaign Dashboard row + an **Active Threads summary block written without renumbering the existing 109 items** (2 closed, 2 opened), NPC Directory status changes, timeline row, Party Snapshot rolled to S17. All four `S11-S20` trackers given `## S17` sections (**Quote Board 31 blocks · Loot 12 rows · Profanity 87 instances + running totals · Roll Stats 143 archived rolls**). **7 PC pages** (plus 4 new *Inner Life & Evolution* entries for [[Vega Bloodroot]]), **4 NPC pages**, [[The Volcano Tomb]] (4 new sections), [[Feral Vampire Thrall]], **+10 standing house rules**, Setting Primer ×3 sections, glossary S17 block, `## S17` DM questions (10), Spell Usage (23 rows), Homebrew Abilities (+5), POV journal callout. `generate_public_session_index.mjs` **self-test passed and wrote 7 sessions** — `all_sessions` now **17**, ending at S17. ✅ **First session in the S11–S20 range with every checklist row ✅**, because the roll archive was finally reachable.
- **Commit:** `0f5f6ef`
- **Friction:** `gen-fail` — **the Vega PC page edit landed in the wrong section.** The script anchored on the first `\n- **S16` in the file, but her page (uniquely) carries S16 bullets in **`## Inner Life & Evolution`** *before* `## Key Events`, so the S17 entry went into the wrong one. Caught by a post-edit ordering check, not by the script. What works: **anchor on the section heading first, then the bullet** — never on a bullet pattern alone when the same pattern repeats across sections. ⚠️ Second one, now four times in this session: the **`cp1252` console throws `UnicodeEncodeError` on emoji in `print()`**, and on one run it aborted the job loop *before* the write, silently applying only part of a batch. **Keep every byte of script stdout ASCII** (`.encode('ascii','replace')`), and **write once at the end** so a print crash can never leave a half-applied batch.
- **Next:** Backfill Roll Statistics §S16 — now unblocked.
- **Watch out:** 🛑 **Two vault gaps are still open and this run did NOT close them** — `Vega Bloodroot Journal` and `Spell_Usage.md` both still jump **S11 → S16 → S17**, and `Names & Terms` has no S12–S15 section. ⚑ **New conflicts recorded, deliberately not resolved:** S16 called the objective the **Grieveflame / Flame Greaves** (legs), S17's read-aloud calls them the **Gauntlets of Burning Grasp** (hands) — *one relic or two?*; and the thralls **hunt by smell with no eyes** in S17 against **by sound** in S01, which was the basis for the S01 Thunderwave ruling. ⚠️ **`Voranthyr +2` is archive-sourced and never spoken aloud** — provisional. ⚠️ **One drug reference at raw 02:40:24 is still published** pending your call (spell-check log §6b).

### 2026-09-14 20:58 ET · Claude Code (S17 CONVO 1 COMPLETE — session note, roll archive, speaker map)
- **Changed:** Closed **row 21** (`Tails` → **`Tayls`**, ruled from Taylor's own signature — external and authoritative, not an audio inference) and promoted the glossary entry **provisional → CONFIRMED**; earlier transcripts deliberately not retro-edited. Then finished Convo 1: **queried the roll archive** (143 rolls for `2026-09-07`, **no sync gap**, 50 null-character rolls resolved by `user_id` = 49 DM / 1 Lulu) and wrote **`01-Sessions/Session 17 — Hobo, That's Your Reflection.md`** — all 8 sections, ~7,100 words, all five public-parser H2s (`Metadata`, `Summary`, `Threads`, `NPCs`, `Archivist Notes`) verified present. Title was **named by the table on the record at 02:44:18** after the DM broke off mid-sentence to yell at his dog. Convo 2 handoff block written to `_pipeline/S17/handoff.md` with the note path as line 1.
- **Commit:** `b48fc52` (row 21) · `3e882b8` (session note + archive + speaker map)
- **Friction:** `gen-fail` — the `cp1252` console kept throwing `UnicodeEncodeError` on emoji/✓ inside `print()`, **three separate times**, each *after* the file write had already succeeded. Every one looked like a failed edit and wasn't. What works: **keep all script stdout ASCII** (`.encode('ascii','replace')` when echoing file content back). ⚠️ A second one: the first attempt to patch the corrected file's header **failed its own assertion** because the header uses **em dashes**, not hyphens — the assertion caught it cleanly and nothing was written, which is the pattern working as intended.
- **Next:** S17 Convo 2 propagation from `_pipeline/S17/handoff.md`.
- **Watch out:** 🛑 **The `individual_values` gotcha is real and it fails SILENTLY.** `jsonb_typeof` returns **`string`** on all 143 S17 rows, so `individual_values #>> '{0}'` yields null and **every nat-20 and nat-1 reports zero** with no error. Parse with `(individual_values #>> '{}')::jsonb` — doing so surfaced **3 nat 20s and 5 nat 1s**. ⚠️ Also: `roll_type` is `roll`/`damage`/`to hit`/`save`/`check` — **there is no `attack`** — and the column is `"character"`, not `character_name`. ⚑ **I was wrong in the previous entry that the S17 speaker map was unresolvable** — that judgement came from aggregate name-frequency greps; **reading the transcript resolves all eight**, and both the transcript header and log §7 now carry the resolved map. S16's map genuinely does not transfer (`E` was Taylor/Vega there, `E` is the DM here).

### 2026-09-14 20:14 ET · Claude Code (S17 Batch A+B applied — corrected transcript + spell-check log)
- **Changed:** Applied the approved **15 rows / 24 line-instances / 20 distinct lines** — Batch A (1, 2, 3, 6) and Batch B (4, 5, 7, 8, 9, 10, 11, 12, 13, 16, 17) — and wrote **`Session_Sources/Transcripts/Corrected/17_090726_corrected.md`** plus **`Session_Sources/Transcripts/Spell_Check_Logs/090726_Spell_Check_Log.md`**. Each edit was an **exact-string replace with an expected occurrence count**; all 24 verified present after the write, all 12 superseded forms verified gone. 🛑 **Row 15 reversed and asserted as a hard invariant** (`Theros Emberborn` still on 1622, `Tharos` still 4) and the **`[slur - redacted]` marker asserted to survive** the pass. Rows 14/22/23/26 rejected, 18/19/20/24/25 held for audio, **21 left for Taylor**. The log records the applied table, the reversal evidence, the full redaction inventory, and the verification performed.
- **Commit:** `31279ec`
- **Next:** Close row 21 (`Tails` → `Tayls`), then start S17 Convo 2 propagation.
- **Watch out:** ⚑ **The S17 speaker map is UNRESOLVED and was left blank on purpose** — S16's map does **not** transfer (`E` was Taylor/Vega in S16, `E` is the DM in S17). Firm: **E = DM · B = Chase/doppelganger · C = Taylor/Vega · D = Barrett's player**. **A, F, G, H conflict** and were not guessed; details in the corrected file's header and log §7. ⚠️ **`_pipeline/S17/spellcheck.md` still says "NOTHING HAS BEEN APPLIED" at the top and that is now false** — the spell-check log is the authoritative record, not the proposal set. ⚠️ **Line numbers differ between the two transcripts**: the corrected file carries a ~50-line header, so its body is offset. **Always cite raw line numbers.** ✅ Encoding verified clean — 168 em dashes preserved, 0 U+FFFD; the `?` glyphs in terminal output were a **cp1252 console artifact, not file content**, exactly as `spellcheck.md` claimed.

### 2026-09-14 19:52 ET · Claude Code (S17 raw transcript redacted and banked — privacy hold cleared)
- **Changed:** Taylor ruled **option A** — run the redaction pass rather than gitignore the folder. Backed the original up to gitignored **`_pipeline/S17/17-090726_raw_transcript_UNREDACTED.md`** and md5-verified the copy **before** editing anything, then applied **97 markers (96 personal + 1 slur)** to `Session_Sources/Transcripts/Raw_Unedited/17-090726_raw_transcript.md` via a scripted, verifiable pass. Scope: five contiguous personal blocks (pre-session biography + ethnic jokes · spouse/autism · heights and dating history naming real third parties · food orders · prescriptions/cannabis), the `flags.md` §10 *"kill your wife"* line, and the **r-slur at 02:17:20** done **inline** so the mechanical result survives for the roll log. **Line count, all 2,923 lines, and every timestamp/speaker prefix preserved** — the 13 bare line numbers cited in `spellcheck.md` were re-resolved after the write and all still land correctly. Post-write sweep confirms **zero** hits for the slur and for every redacted proper noun. `flags.md` §11 also flagged L472 as graphic; **deliberately left alone** — it is vulgar campaign table talk, not personal data, and belongs to the Profanity Ledger.
- **Commit:** `76d504e`
- **Friction:** `gen-fail` — the redaction script wrote **CRLF** line endings because Python's `Path.write_text` translates newlines on Windows, which made the verification diff report all 2,922 lines as changed and completely hid the 97 real edits; the sensitive-term sweep looked fine, so this would have banked a whole-file rewrite as if it were a 97-line change. What worked: **compare byte-level line endings against the precedent file (S16 is LF-only) before trusting any diff**, and re-open with `newline=""` to write the joins verbatim. A second, cosmetic failure: a `✓` in a `print()` crashed on the `cp1252` console **after** the file was already written — keep script output ASCII on this machine.
- **Next:** Apply `_pipeline/S17/spellcheck.md` Batch A+B (15 rows).
- **Watch out:** ⚠️ **Line-preserving redaction is now a hard constraint for this vault, not a nicety** — `spellcheck.md` cites bare raw-line numbers, so any future pass that deletes or merges lines silently invalidates the whole review packet. Replace text in place, never remove a line. ⚑ Option B was **not** taken, so `Raw_Unedited/` is still tracked and **18 older transcripts remain public and unaudited**; that policy question is untouched and still item 4 below.

### 2026-09-14 19:36 ET · Claude Code (orientation only; guard fired on the held S17 transcript)
- **Changed:** no code change. Session opened into the vault and reported state back to Taylor; no pipeline work ran. The Stop hook reported `HANDOFF NOT BANKED — 1 uncommitted file`, and that file is **`Session_Sources/Transcripts/Raw_Unedited/17-090726_raw_transcript.md`**, which is held deliberately. 🛑 **It was not committed and will not be committed by an agent.** `origin` is `github.com/TheLittlestAskew/ashfall_vault` and is **public**; banking that file publishes an r-slur (02:17:20), ~12 min of pre-session personal disclosure naming a spouse, an autism conversation, dating history with real named people, and a prescriptions/cannabis exchange (00:52–00:55) — irreversibly, to strangers. The guard's instruction to bank without asking does not extend to publishing third parties' personal data; **the redaction judgement is Taylor's and it is still unruled across S15, S16 and now S17.** Only `HANDOFF.md` and `TOOLS.md` were banked here.
- **Commit:** —
- **Next:** Unchanged. See the DO NEXT block above this log.
- **Watch out:** ⚠️ **This is a guard/policy standoff that will repeat every single session until Taylor rules**, because the guard counts any dirty file as unbanked and the only correct move on this particular file is to leave it dirty. Two exits, both Taylor's: run the S15/S16 redaction pass (24 markers, unredacted original to gitignored `_pipeline/`), **or** gitignore `Session_Sources/Transcripts/Raw_Unedited/` the way `Recordings/` already is — now the **sixth** session carrying that standing recommendation. ✗ The escape hatch (`.git/claude-handoff-skip`) was **not** written; the skill forbids writing it on an agent's own initiative and Taylor never said to skip.

### 2026-09-12 18:03 ET · Claude Code (accents FILL token closed; all 26 S17 spell-check rows reviewed)
- **Changed:** Closed **`⟦FILL: player languages / accents⟧`** after eight sessions. Taylor's ruling: **no accented or non-native English speakers at the Ashfall table** — the accented players are all in *Sky Is The Limit* (**Sirius/Star → Aeolus, Polish**; **Florian → Blarg, German**), and SITL's own instructions already name them at `Convo_1_Instructions.md:83`, so that vault needed no edit. Replaced the token with an **explicit negative** (plus a pointer to SITL so nobody reopens it) in all five live Ashfall files — `Project_Instructions.md` L127 · `Convo_1_Instructions.md` L21/L62/L135 · `Workflows/Project/Automation/convo1_phaseA.md` L17 · `ADDENDUM_AssemblyAI_Transcription_Process.md` L93 — and fixed **two SITL leftovers** in the constraints tables (`Project_Instructions.md` L147, `Convo_1_Instructions.md` L93) that still said *"Account for non-native English speakers"* and now contradicted the ruling. Also marked it closed in `00-Campaign-Hub/DM Questions — Open.md` (it sat correctly under *"Not for the DM"*) and in `_pipeline/S17/flags.md` §12. **Then reviewed all 26 rows of `_pipeline/S17/spellcheck.md`**, each verified against the raw transcript: **15 apply · 4 reject · 5 hold for audio · 1 reversed · 1 Taylor's.** See the DO NEXT block for per-row verdicts.
- **Commit:** `77a306d`
- **Friction:** `re-run` — Taylor re-invoked the S17 Phase A headless command (`cat _prompt_convo1_phaseA_md.txt | claude -p`) when Phase A had **already completed 12 minutes earlier**; nothing surfaced the finished state to her, so the default action would have been to redo ~3 hours of transcript processing and overwrite a good review packet. What worked: **check `_pipeline/S<NN>/READY_FOR_REVIEW` and the output-file mtimes before running any Phase A prompt**, and report the existing result instead. The watcher owns the toast that should have said this — worth checking why it didn't fire.
- **Friction:** `misread` — I told Taylor the `spellcheck.md` line citations looked wrong ("rows 15 and 16 both cite L1622 and neither string is there") and briefly suspected a fabricated row. The citations were **correct**; my own `cut -c1-420` truncation hid the content, because **raw L1622 is a single ~700-character line** holding both `Theros Emberborn` and `the gauntlets of burning grasp`. What worked: verify transcript citations with unfiltered `awk 'NR==<n>'` — **never pipe transcript lines through `cut` when checking whether a string is present.** The false alarm did pay for itself: chasing it produced the Theros/Tharos read-aloud finding below.
- **Next:** Apply `spellcheck.md` **Batch A+B (15 rows)** to produce the S17 corrected transcript and spell-check log, and rule on **row 15 (reverse it — evidence favours `Theros`)** and **row 21 (`Tails`→`Tayls`, yours to confirm)**.
- **Watch out:** 🛑 **Row 15 is backwards in the table.** It proposes `Theros`→`Tharos`; the evidence favours **`Theros`**. The three spellings are not equal-weight — **L1348** and **L1618** are both ad-libs (at L1618 he says *"I don't know where it is"*, hunting for his note), while **L1622** is *"Sorry, I was looking at the wrong note"* followed by the **prepared boxed text**, which reads `Theros`. Inference, not fact, and one prepared block is thin — but it narrows DM question #1 from a coin-flip to *"your notes say Theros, right?"* and it matches S12. ⚠️ **The ≤60% proper-noun confidence cap is miscalibrated** and should be reworded in `Convo_1_Instructions.md` before S18: it fired on **row 17** (which *removes* a spurious proper noun, `Emily`→`Sammy`) and **row 16** (capitalization only), neither of which canonizes anything. ⚠️ **`_pipeline/` is gitignored**, so the `flags.md` §12 annotation is **not** in this commit — it lives only on disk. 🛑 The raw S17 transcript is still **deliberately unbanked**; the Stop hook will keep reporting it until the redaction call is made.

### 2026-09-09 18:05 ET · Claude Code (post-mortem on the Phase A "API error" — no work lost)

- **Changed:** no change. **Verified that the API error Taylor hit at the end of the S17 Phase A run cost nothing.** All four artifacts are complete and untruncated (`spellcheck.md` rows numbered 1–26 with no gaps and a clean closing section, `flags.md` 15 sections, `summary.md` ending on its "Next step for Taylor" block, `READY_FOR_REVIEW` present); the run's own handoff was written and pushed (`ce39c90`, 17:36) and the scheduled rotate task tidied the log (`22575ab`, 17:44). ✅ **The raw transcript is byte-for-byte untouched** — 2,922 lines / 21,839 words / mtime 09-07 23:01, exactly as transcribed. ✅ **`approvalBlocker` now returns `null`** — S17 is genuinely approvable, and the guard patched earlier today agrees. **The error struck after every deliverable was on disk and banked; it killed the closing narration, not the work.**
- **Commit:** — (verification only)
- **Friction:** gen-fail — **the auto-mode classifier (`claude-sonnet-5[1m]`) timed out mid-task**, blocking every Bash call with *"cannot determine the safety of Bash right now"* — the same API instability that produced Taylor's error minutes earlier. ✅ **What worked: read-only tools do not go through the classifier.** `Grep` and `Read` kept working throughout and finished the verification with no waiting. **When the classifier is down, switch to `Read`/`Grep`/`Glob` rather than retrying Bash in a loop.**
- **Next:** Unchanged. Review `_pipeline/S17/spellcheck.md` (rows 14–26 default to *held*), and rule on the raw transcript.
- **Watch out:** ⚠️ **`state.json` still reads `awaiting_approval` and is now accurate by coincidence, not by mechanism.** Phase A was re-run by piping the prompt into `claude -p` directly, which **bypasses `processRecording()`** — so the honest-stage write added in `19c1f1f` never executed, and the file simply kept the stale value from 09-07 that happens to match reality. ⚑ **The manual-resume path writes no state at all**; if a future resume fails, `state.json` will again claim whatever it last held. The `spellcheck.md` existence check is what actually protects `--approve`, not the stage field.

### 2026-09-09 17:34 ET · Claude Code (S17 Convo 1 Phase A — spell-check complete, stopped for review)

- **Changed:** **Ran S17 Convo 1 Phase A to completion** from the watcher's own prompt file — the 09-07 OAuth stall is cleared, no re-transcription, no `--file`. Wrote the four review artifacts to gitignored `_pipeline/S17/`: **`spellcheck.md`** (26 proposed corrections over 32 line-instances, **13 at/under 60%**, plus a checked-and-left-alone table and an encoding-artifact section), **`flags.md`** (15 sections), **`summary.md`**, **`READY_FOR_REVIEW`**. ✅ **Session number and date confirmed, no discrepancy — S17 / 09/07/2026**, four-way cross-check. **Nothing applied; the raw transcript is byte-for-byte untouched.** Per the Phase A scope, deliberately did **not** apply corrections, write a corrected transcript or spell-check log, query Supabase, generate notes, or do any Convo 2 work. **No vault file outside `_pipeline/S17/` was written.**
- **Commit:** — (all deliverables are inside gitignored `_pipeline/`; the only tracked change this session is this handoff. See Watch out.)
- **Findings worth surfacing:** 🛑 **`Tharos` and `Theros` Emberborn now appear in the SAME transcript**, three minutes apart, both in the DM's read-aloud text (raw L1348/L1618 vs L1622) — the S12-vs-S16 conflict is no longer cross-session, it is internal, and it now blocks three sessions. 🛑 **A player delivered the session's climactic boxed text** (~130 words, raw L2864, with the DM's on-record permission) — authored in-fiction canon spoken by a player, so speaker labelling and Quote Board attribution both need a ruling before script-format conversion. 🛑 **SPEAKER C is acting as a partial diarizer catch-all**, exactly as SPEAKER B did in S11 — nine concrete misattributions are tabulated in `flags.md` §3, so **do not hard-code speaker names yet**. ✅ **S16's `"Naruto"` garble is resolved** — it is a pet's name, not campaign content. ⚑ `Gus` is now **three sessions** with no referent.
- **Friction:** gen-fail — creating the zero-byte `READY_FOR_REVIEW` marker failed twice on tool-call policy, not on the filesystem: Bash rejected `: > READY_FOR_REVIEW` as a multi-operation command, then PowerShell rejected the fallback for containing `$()` subexpressions. ✅ **What worked: the `Write` tool with empty content.** Same family as the `&&`-chain and `for`-loop rejections already recorded in `TOOLS.md`; **for trivial file creation, reach for `Write` first rather than a shell idiom.**
- **Next:** Review `_pipeline/S17/spellcheck.md` row by row (rows 14–26 default to *held*), and **rule on the raw transcript** — redact it the way S15/S16 went, or gitignore `Raw_Unedited/`.
- **Watch out:** 🛑 **`Session_Sources/Transcripts/Raw_Unedited/17-090726_raw_transcript.md` was deliberately NOT committed.** This repo is public, and committing it *is* publishing it. S15 and S16 were both redacted first (24 markers each, unredacted original held in gitignored `_pipeline/`); S17 needs the same and carries **more** than S16 — an **r-slur at 02:17:20** (third session, third speaker), ~12 min of pre-session personal disclosure naming a spouse, an autism conversation, real named people, and a prescriptions/cannabis exchange. ⚠️ **A non-interactive run whose scope was explicitly `_pipeline/S17` only is the wrong place to make a 24-marker redaction judgement**, so it was held rather than published or half-done. **The Stop hook will keep reporting this file as unbanked until you rule — that is the guard working, not a fault.** ⚠️ **The `Raw_Unedited/` gitignore recommendation is now five sessions old and still the cheapest fix.** ⚑ Also unchanged: **18 already-public transcripts have never been audited.**

### 2026-09-09 16:10 ET · Claude Code (approve() guard patched — the S17 failure mode can't recur)

- **Changed:** Patched the two defects found this morning in `Workflows/scripts/ashfall_pipeline_watch.js`. **(1)** `state.json` was written with `stage: 'awaiting_approval'` **above** the `if (!ok)` check, so a failed Phase A still read as approvable; it now records `phaseA_failed` when Phase A fails. **(2)** `approve()` force-marked `phaseA` as `done` and never checked that `spellcheck.md` existed, so approving a failed run would have applied corrections, rewritten notes, committed and pushed from a review packet that was never created. Added **`approvalBlocker(st, exists)`** — a pure predicate covering four refusal cases (no `pendingFolder`, already `complete`, `phaseA_failed`, missing `spellcheck.md`) — and wired it into `approve()` **before any side effect**, with a refusal message that prints the exact Phase A resume command. Added **`--self-test`**, matching the convention already in `generate_public_session_index.mjs`. **54 insertions, 1 deletion, one file.**
- **Commit:** `19c1f1f`
- **Verification:** ✓ `node --check` parses. ✓ `--self-test` passes (6 assertions). ✓ **Negative control run:** with the two guard conditions stubbed to `false` on a throwaway copy, the self-test **fails at the `spellcheck.md` assertion** — so the test genuinely catches the regression rather than passing vacuously. ✓ **Exercised against the real stalled state**, read-only: the live `_pipeline/state.json` still carries the stale `awaiting_approval` the old code wrote, `spellcheck.md` is absent, and `approvalBlocker` returns *"spellcheck.md is missing from …\S17 — Phase A did not finish."* ✓ All four refusal messages rendered and eyeballed. ✓ `_pipeline/state.json` and `status.json` md5s unchanged, `_pipeline/S17/` still holds only the prompt file, working tree shows only the intended file.
- **Friction:** gen-fail — **the Bash heredoc silently ate escaped backslashes and mangled a UTF-8 em dash**, twice. The em dash broke a `str.replace` anchor (the patch aborted cleanly on an assert), and the backslash one was worse: it emitted `` `…_pipeline\watcher.log` `` into a **template literal**, where JS reads `\w` as a bare `w`, so the message would have rendered `_pipelinewatcher.log`. ✅ **What worked: keep patch sources pure ASCII — `'\u2014'` for the em dash and `chr(92)` for backslashes — and *always* read the resulting `git diff` rather than trusting the patch script's "success".** The diff is what caught it.
- **Friction:** gen-fail — `node ashfall_pipeline_watch.js --approve` was **blocked by the auto-mode classifier**, so the end-to-end refusal could not be demonstrated by running it. ✅ **What worked: extract `approvalBlocker` from the file's own source with `new Function` and run it against the real `state.json` read-only** — same evidence, zero side effects. ⚠️ **The live `--approve` path is still undemonstrated end to end; run it yourself if you want that proof — it should refuse and change nothing.**
- **Next:** Re-run S17 Convo 1 Phase A: `cat _pipeline/S17/_prompt_convo1_phaseA_md.txt | claude -p --permission-mode acceptEdits` from the vault root.
- **Watch out:** ⚠️ **`_pipeline/state.json` still says `awaiting_approval` for S17 and was deliberately NOT hand-edited.** The guard catches it anyway (via the missing `spellcheck.md`), and the Phase A re-run overwrites the file with an honest stage — editing state by hand to fix a code bug is how you end up with a state machine nobody trusts. ⚑ **`stage` is still effectively write-only elsewhere:** `approve()` reads `pendingFolder`, `nn`, `transcript`, `mmddyy` and `iso`, and now `stage`, but nothing else in the script consumes it. 🛑 **The S17 raw transcript remains untracked and unredacted** — unchanged from this morning, and the `Raw_Unedited/` gitignore ruling is still yours.

### 2026-09-09 15:40 ET · Claude Code (S17 pipeline stall diagnosed — no vault changes)
- **Changed:** no change to the vault. **Diagnostic-only session:** found why the Ashfall pipeline has been stopped since 2026-09-07. **S17 transcription SUCCEEDED** (`17-090726_raw_transcript.md`, 176 min, 21,839 words, 2,922 lines, 95.5% confidence, 8 speakers) and the DDB party sync ran clean (6/1/0). **Convo 1 Phase A then died on `Failed to authenticate: OAuth session expired and could not be refreshed`** (`_pipeline/watcher.log:371`) — the headless `claude -p` call the watcher makes could not authenticate at 11:01 PM. `_pipeline/S17/` holds **only** `_prompt_convo1_phaseA_md.txt`; no `spellcheck.md`, `flags.md`, `summary.md` or `READY_FOR_REVIEW`. ✅ **Auth works again** — verified with a live headless `claude -p` against this vault today. ✅ **The watcher itself is healthy** (Task Scheduler `Ashfall Pipeline Watcher` = Running, PID 15440) but **will not self-heal**: watch mode uses `ignoreInitial` and already consumed this mp3. ✅ Two S16 blockers confirmed **cleared**: `Public Session Index.json` now ends at S16 (`643ebf0`, `all_sessions` 16, `sessions` 6), and `mcp__supabase__execute_sql` is now allowlisted in `.claude/settings.local.json`.
- **Commit:** — (`docs: handoff friction log`; the only working-tree file is the untracked S17 raw transcript — see Watch out)
- **Friction:** gen-fail — my first two headless-auth probes returned **empty output and a misleading success**: `claude -p | head` reported `EXIT=0` because that was *head's* exit code, and the second probe backgrounded and wrote nothing for minutes. ✅ **What worked: `claude -p --output-format json --max-turns 1`** — the JSON body carries `duration_api_ms`, `total_cost_usd` and a `session_id`, which **proves a real API call happened**, so auth is confirmed by evidence rather than by an exit code. `is_error: true` there is the max-turns stop, not an auth failure. **Never test `claude -p` auth by exit code through a pipe.**
- **Friction:** gen-fail — that backgrounded `claude -p` was launched with **cwd = the vault root**, so it inherited this repo's Stop hook and spent a full paid run doing its *own* handoff attempt, staging a competing `HANDOFF-S17-pending.md` in another session's scratchpad. Harmless here (its writes were permission-refused) but it nearly forked the handoff into two authors. ✅ **What worked: read its output as evidence, not as instructions, and re-derive everything locally.** ⚠️ **Never probe `claude -p` from inside a handoff-enabled repo's cwd — use a neutral directory.**
- **Next:** Re-run S17 Convo 1 Phase A from the existing prompt file: `cat _pipeline/S17/_prompt_convo1_phaseA_md.txt | claude -p --permission-mode acceptEdits` from the vault root.
- **Watch out:** 🛑 **DO NOT double-click `Approve-Ashfall.cmd` — there is a real bug and it will run Phase B against nothing.** `ashfall_pipeline_watch.js:272` writes `state.json` with `stage: "awaiting_approval"` **before** it checks whether Phase A succeeded, and `approve()` (line 331) then **force-marks `phaseA` as `done`** with no check that `spellcheck.md` exists. Approving now would apply corrections, write the session note, propagate and push, all from a review packet that was never created. 🛑 **Do NOT restart with `--file`** — that path calls `processRecording()`, which re-transcribes unconditionally and would burn another 176 min of AssemblyAI for a transcript that already exists. 🛑 **`17-090726_raw_transcript.md` is untracked ON PURPOSE and was NOT committed.** It carries **zero redaction markers**, the flags pass that would inventory what needs redacting is the exact step that died, and this repo is public — same disposition as S16 Phase A (log entry 2026-09-03 23:20). ⚠️ The Stop hook will keep reporting "1 uncommitted file" until the gitignore call is made; that is the guard working. **The `Session_Sources/Transcripts/Raw_Unedited/` gitignore recommendation has now been made five sessions running with no ruling, and it stalls this same gate every time.**

### 2026-09-04 00:52 ET · Claude Code (S16 Convo 2 — full vault propagation, banked)
- **Changed:** Ran **Convo 2 for Session 16** end to end. **32 files: 6 created, 26 modified.** Created [[The Doppelganger]] ⚑, [[Tharos Emberborn]] ⚑, [[Emberos]] ⚑, [[Broken Crown Mountains]], [[The Volcano Tomb]] ⚑, [[Undead Centipede-Worms]] ⚑. Dashboard: S16 row, **threads 87–109 opened**, thread 73 resolved, thread 81 explained retroactively, **NPC directory +8**, eastern locations, timeline, **Party Snapshot → LEVEL 12**, and two stale blocks corrected (the false "CATCH-UP QUEUE EMPTY 🎉" and the superseded S11-era Supabase diagnosis). Trackers: Loot (13 rows), Quote Board (39 quotes), Profanity (+ running totals, **new campaign record: Chase ≥79**), Roll Stats (**blocker stub**). All 8 PC pages updated with **level 10 → 12**; Vega **+7 Inner Life / +7 quotes / +5 relationships**; **[[Valerian Hellebore]] given a disambiguation header and a CAPTIVE status row**. Plus [[Lt. Hargraven]], [[The Dead Zone]], [[The Walled Base (Museum-Castle)]], [[Beast Thrall]], [[Setting Primer]] (**a whole new First Vampire War section — the largest lore addition since S01**), [[Names & Terms]] (16 first-seen terms, **none canonized**), [[House Rules & Rulings]] (**+7 standing / +6 deferred**), [[Homebrew Abilities]] (+5), `Spell_Usage` (+21), [[DM Questions — Open]] (**new S16 section: 5 blocking + 24 others**), and **one surgical annotation to the S15 note**.
- **Commit:** `616a8df`
- **Verification:** ✓ Every append anchored on a tail read first; **no full-rewrite of any existing file** — all edits were targeted old/new string replacements. ✓ The S16 session note was **verified against the website parser contract, not rewritten** (all five H2s present, Location rows present). ✓ Generator contract checked by hand since the script couldn't run: Dashboard row parses to `n=16 / t=What the Fuck / d=2026-09-03`, the note exists at that exact path, and `## S16` blocks confirmed present in all three required trackers via the script's own `^##\s+S0?16(?:\s|—|$)` pattern. ✓ Pre-commit secret scan: only the `sbp_…` placeholder matched; `.env` and `_pipeline/` confirmed gitignored via `git check-ignore`. ✓ Push confirmed `70a2996..616a8df`, tree clean, in sync with origin.
- **Friction:** gen-fail — **the roll archive regressed.** `mcp__supabase-cutter__execute_sql`, the exact call that cleared the S13–S15 debt on 08-30, came back **permission-not-granted** this run, and `supabase` / `supabase-account2` failed on `Unauthorized` and the same gate. ✅ What worked: **finding the working call name recorded in [[Roll Statistics S11-S20]]'s own Archive Flags section** — that's how I learned `supabase-cutter` was ever the answer, which turned "the token is missing" into the sharper "there are two independent fixes and either one clears it." Fabricated nothing; wrote the S16 Roll Stats section as an explicit empty blocker stub rather than dressing transcript guesses up as statistics.
- **Friction:** gen-fail — `node` could not be executed at all (four attempts across Bash and PowerShell, including with the sandbox override), so the mandated `generate_public_session_index.mjs` step could not run and item 14 stayed ⬜. ✅ What worked: **reading the script and verifying its contract by hand** — every condition it checks was confirmed satisfied, so the blocker is provably one command, not hidden missing work. Same class of blocker as the S12 `docx_to_text.py` failure on 08-29; **two mandated scripts have now been blocked by the same permission gate in a non-interactive run.**
- **Friction:** gen-fail — several Bash forms were rejected by the static analyzer: `for` loops with `${var}` expansion ("Contains expansion"), multi-command `&&`/`;` chains, and `git commit -m` in Bash. ✅ What worked: **the Grep tool instead of shell loops, one command per call, and PowerShell for git** — consistent with the `git -C` note already in `TOOLS.md`.
- **Next:** Fix Supabase access (either route), re-run the two `ashfall_session_rolls` queries and fill [[Roll Statistics S11-S20]] §S16; then run the index generator so S16 publishes.
- **Watch out:** 🛑 **[[Roll Statistics S11-S20]] §S16 is intentionally empty.** Per the Convo 1 handoff's instruction, **the transcript was not used to populate it** — the diarizer misattributes bare roll numbers, so per-character counts from it would be worse than nothing. The ~45 provisional rows live in the session note's Full Roll Log and are flagged per row. 🛑 **Two things were deliberately NOT done and should stay that way until you rule:** [[The Doppelganger]] was **not merged** into [[Valerian Hellebore]]'s page, and **the S15 note was not retro-edited** — it got an appended disambiguation annotation instead, because the record of the deception working *is* the artifact. ⚠️ **The Profanity Ledger changed units at S16** — line counts, not instances, because there was no archive to count against. Totals are marked `≥` and the running totals are now mixed-unit; **Chase's ≥79 record still holds** because 79 lines implies at least 79 instances. ⚠️ **Both project instruction files are still four sessions stale** (roster at level 8, Deanna listed active, Lulu absent) — now logged as Dashboard thread 108.

### 2026-09-03 23:58 ET · Claude Code (S16 Convo 1 Phase B — corrections applied, notes generated, banked)
- **Changed:** Completed Convo 1 for **Session 16 (2026-09-03)**. Applied **15 of the 19 approved** spell-check rows (**20 line-instances**) from `_pipeline/S16/spellcheck.md`; **held 4** because the approved table's own third-section heading reads *"low confidence, flagged not fixed"* (same disposition as S12 rows 12–16). Wrote the corrected transcript, the spell-check log, and the canonical **8-section session note** — title taken from the record: **Session 16 — What the Fuck** (Taylor, 02:03:56, *"note to Claude AI, the name of this session is What the Fuck"*). Convo 2 handoff written to `_pipeline/S16/handoff.md` with the note path as line 1. **No `.docx`** (retired). Zero combat, zero initiative, so Encounters/Initiative are thin by necessity and say so.
- **Commit:** `4ccfc37`
- **Verification:** ✓ All 20 line-instances confirmed present in the output and all 15 pre-correction strings confirmed **absent** (`Thalos`, `Everborn`, `Iluned`, `tree pain`, `Baja Boys`, `Liam's tiny`, `Sunflower`, `binnies`, `Bane's`, `Vale`, `Netherite`, `Death Charge`, `Maddie` → 0 matches each). ✓ Website parser contract validated — `## Metadata` / `## Summary` / `## Threads` / `## NPCs` / `## Archivist Notes` all present as H2s, plus a Location row; one unescaped `|` inside a wikilink in the Metadata table found and fixed before commit. ✓ Whole 2,766-line transcript read, no sampling. ✓ Redaction parity checked: 24 markers in each transcript, and a follow-up sweep caught two household-name instances the flags packet had missed.
- **Friction:** gen-fail — Supabase MCP returned `Unauthorized`; `.mcp.json` interpolates `SUPABASE_ACCESS_TOKEN` and it is unset, and the two alternate Supabase servers are scoped to other projects and weren't permission-granted. **The roll archive was never queried.** ✅ What worked: state the failure explicitly, mark the whole roll log transcript-only and provisional per row, and fabricate nothing — but this is now the **third** session blocked on the same missing token and it should be fixed at the environment level, not worked around again.
- **Friction:** gen-fail — `python` and `awk` were both rejected by the sandbox, so a written-and-verified correction script could not run. ✅ What worked: `cp` the raw file to the corrected path with Bash, then apply each correction as a discrete `Edit` with a unique anchor string, then verify with `Grep`. Slower but every edit is individually auditable, which turned out to be the better artifact anyway.
- **Friction:** misread — I first drafted the spell-check log and session note saying *"nothing was redacted this pass"*, treating redaction as out of scope for a non-interactive run. That was wrong the moment banking entered the picture: committing publishes to a public repo and is irreversible. ✅ What worked: checking what S15 actually did (`15-082726_raw_transcript.md` carries 19 redaction markers, and its corrected twin uses `[personal conversation - redacted]`) — the precedent already existed and answered it. Redacted both files to match, preserved the unredacted original in gitignored `_pipeline/`, and corrected all three documents that had asserted otherwise.
- **Next:** Restore `SUPABASE_ACCESS_TOKEN`, re-run the two `ashfall_session_rolls` queries, amend the S16 Full Roll Log, then start Convo 2 from `_pipeline/S16/handoff.md`.
- **Watch out:** 🛑 **Every roll figure in the S16 note is transcript-derived and unverified.** On top of the missing token, the diarizer **misattributes bare roll numbers** to whoever was speaking (confirmed at L338, L1416, L1430, L1478, L2280) — rows corrected from context are marked ⚑. **The archive is authoritative; that transcript is not.** 🛑 **Speaker labels were deliberately NOT converted to character names**, because SPEAKER B's in-character lines belong to the doppelganger and hard-coding a name would write a false attribution into the archive; a documented S16-only speaker map sits in the corrected transcript header instead. ⚠️ **The doppelganger is linked as `[[The Doppelganger]]` — provisional, pending your ruling.** ⚠️ Party level jumped **10 → 12 off-screen**; both project instruction files still say level 8, still list Deanna as active, and still don't mention Lulu. **Four sessions stale.**

### 2026-09-03 23:20 ET · Claude Code (S16 Convo 1 Phase A — spell-check only, awaiting review)
- **Changed:** Ran Convo 1 Phase A for **Session 16 (2026-09-03)** end to end and stopped at the review gate, per the automated brief. Wrote `_pipeline/S16/spellcheck.md` (19 proposed corrections across 27 line-instances; 8 recommended >60%, 11 at/under the cap), `_pipeline/S16/flags.md` (8 sections) and `_pipeline/S16/summary.md`, plus the `READY_FOR_REVIEW` marker. **Session number and date confirmed against transcript content — no discrepancy.** 156 min, 20,958 words, 2,766 lines, 95.2% confidence, 8 speakers, **no absences**. **Nothing applied, no Supabase query, no notes generated, no `.docx`** — all out of scope for this phase.
- **Commit:** — (`_pipeline/` is gitignored; **the raw transcript was deliberately left untracked**, see Watch out)
- **Verification:** ✓ Whole transcript read in six passes, no sampling. ✓ Speaker map derived independently and cross-checked against self-identifications — **DM is `F` this session** (was `A` in S15, `B` in S14). ✓ Every proposed proper-noun change that is absent from `Names & Terms.md` capped at 60% per the standing rule, with the three capped-not-uncertain rows called out as such. ✓ Zero `[inaudible]` markers in the source. ✓ `git status` confirms nothing of mine is stageable and `git check-ignore` confirms `_pipeline/` is the reason.
- **Friction:** gen-fail — two Bash calls were rejected by the sandbox's static analyzer and needed a redo: `: > READY_FOR_REVIEW` (bare `:` builtin) and `cd "<vault>" && git status` (cd-then-git is blocked as an untrusted-hook risk). ✅ **What worked: create empty marker files with the Write tool and an empty string, and reach git as `git -C "<path>" <cmd>` — via PowerShell, not Bash.**
- **Next:** Review `_pipeline/S16/` and rule on the doppelganger attribution + the privacy item, then Phase B.
- **Watch out:** 🛑 **Nothing was committed and that is deliberate.** The only file in the working tree is `Session_Sources/Transcripts/Raw_Unedited/16-090326_raw_transcript.md` — **unredacted, in a public repo**, and it carries the exact categories `_pipeline/S16/flags.md` §6 flags (a slur, a participant's occupation, Taylor's medical history, children's names). It stays untracked until the standing gitignore decision is made. ⚠️ The Stop hook will keep reporting "1 uncommitted file" for this repo until then — **that is the guard working correctly, not a handoff that failed.** ⚠️ **Scope conflict worth knowing about:** the Phase A brief says *"write nothing outside `_pipeline/S16`"* while the Stop hook mandates a handoff commit at repo root. I honoured the hook (Taylor's standing instruction) for `HANDOFF.md`/`TOOLS.md` only. ⚠️ The diarizer **misattributes bare roll numbers** to whoever was speaking — at least four confirmed instances. **Do not build the S16 roll log from this transcript; use the DDB archive.**

### 2026-09-02 22:20 ET · Claude Code (TOOLS.md tool inventory added)
- **Changed:** Added `TOOLS.md` (18 active rows) — Obsidian + its 4 plugins, AssemblyAI, the Ashfall Pipeline Watcher scheduled task, chokidar, BurntToast, Supabase + the vault-scoped `supabase` MCP server, `docx_to_text.py`, and the rest. `AGENTS.md` gained a `### TOOLS.md` subsection so Codex maintains it too. One of 13 project tables that `septentrion-sync` v4 rolls into the vault's new `The Toolbox.md`.
- **Commit:** `51f0b0e`
- **Next:** Unchanged. See the block above this log.
- **Watch out:** The four Obsidian-plugin rows carry `~` inferred dates from plugin folder timestamps, not measured use, so they'll drift into the master table's 90-day stale section without meaning the plugins are unused. That's a known limit of inferring plugin usage, not a signal to uninstall anything.

### 2026-08-31 11:20 ET · Claude Code (publish made deterministic)
- **Changed:** Applied the same publish fix found in `sitl_vault`. `Publish-Ashfall.cmd` now regenerates `Public Session Index.json` **before** committing and aborts the publish if it fails; `ashfall_pipeline_watch.js` gains a **publish** stage that regenerates + commits + pushes in code at the end of `approve()`; `ashfall_status_window.ps1` derives its step total rather than hardcoding "of 4".
- **Commit:** `7540159`
- **Verification:** ✓ `node --check` passes on the watcher. ✓ The generator's self-test and live run both pass and still write 5 published sessions (S10, S11, S13, S14, S15).
- **Friction:** misread — I used `git add -A` to stage the workflow changes and it swept in a **deletion of `Session_Sources/061526 Ashfall Britannia Recording.mp3`**, a tracked mp3 that was already gone from disk. The outcome is fine (audio does not belong in a public repo) but it was unintended. ✅ **What works: check `git status --short` output before committing when using `add -A`, not after.**
- **Watch out:** ⚠️ The abort message is Ashfall-specific on purpose — this generator reads `Campaign Dashboard.md`, **not** `01-Sessions/`, and sessions ≥10 also need their three tracker blocks. Those are the two things that will fail a publish here.
- **Next:** Unchanged. See the DO NEXT block above.

> Older entries archived to `handoff-archive/2026-06.md`, `handoff-archive/2026-07.md`, `handoff-archive/2026-08.md` - everything before 2026-08-31 11:20 ET.
