# HANDOFF — ashfall_vault

> Obsidian D&D vault for the "Ashfall Britannia" campaign (Tayls is a PLAYER, not the DM). Includes ddb-roll-sync and Site_HTML.
> Handoff is **enabled** for this repo. Every change updates the DO NEXT block below and prepends a log entry.
> Note: this is a notes/content vault — most session-note edits won't have a "next dev step." Use the DO NEXT block for things like next-session prep if useful, or leave it as "—".

## ▶ DO NEXT — 🛑 A DUPLICATE TRANSCRIPT IS IN THE PIPELINE. Resolve that first.
🛑 **0. `13-062226_raw_transcript.md` IS NOT SESSION 13.** It is a **second AssemblyAI run of Session 11's audio** — identical `# Source: 062226 Ashfall Britannia Recording.mp3`, same 137-min duration, same model, same 95.1% confidence, same first and last lines; only the transcription timestamp differs (2026-08-29 vs 2026-08-03). S11 is already complete end to end. The file is banked as evidence in `0802563` — **not renamed, not deleted, not processed.** Full evidence table in `_pipeline/S13/summary.md`.
> **The re-run is not simply worse, which is why it wasn't discarded:** diarization *improved* (SPEAKER B 131→81 turns, G 78→96 — partially fixing the S11 "catch-all" flag that still blocks script-format conversion), `Beastrell C` is fixed, and the unresolved glossary term **`Hunter` does not appear anywhere in it** (the same 20-second window transcribes as ordinary combat) — good evidence it was a first-run hallucination worth retiring from `Names & Terms`. It also **regressed** `Beast Thrall F` → `Beast Thrall Elf` at 01:43:32.
>
> **Decide:** merge/replace the S11 raw transcript with this run, or discard it — then **find the actual Session 13 recording.** Sessions run weekly (S10 061526 · S11 062226 · S12 062926), so S13 should be ~**070626**. ⚠️ Until this is resolved, two files with the same audio and different session numbers sit in `Raw_Unedited/` and will confuse the watcher.

⚠️ **0b. S12 looks unfinished.** `_pipeline/state.json` still reads `stage: awaiting_approval` pointing at S12, and `01-Sessions/Session 12 — Only the Relics Can End Their Rule.docx` is a **`.docx`** in a folder where every other session is markdown — with `.docx` generation **retired 06/06/2026**. There is no `Session 12 — ….md`. Not touched this run.

✅ **S11 is done end to end.** Convo 1 (`4ad9012`) and Convo 2 (`84dd76e` + `5696807`) are both written and pushed. **7 files created** — the whole `S11-S20` tracker set plus [[The Dead Zone]], [[The Cloaked Figure]], [[The Massive Thrall]] — and **24+ updated**. Item-by-item status is in `00-Campaign-Hub/Vault Sync Status.md` under the **S11 matrix**. Every checklist row is ✅/➖ **except Roll Stats (⚠) and the website (⬜)**.

🛑 **1. FIX ONE LINE IN `.claude/settings.local.json` — this is the highest-value thing on this page.** It allowlists `mcp__plugin_supabase_supabase__execute_sql`, which matches **no connected server**. The live tool is **`mcp__supabase__execute_sql`**. *(Confirmed this run: the `supabase` MCP server connected mid-session and exposed exactly that name; the permission prompt was raised against the correct tool and denied purely because the allowlist string doesn't match.)* **Five consecutive automated runs have now been blocked by that one typo.** Fix it, then run **once, interactively**:
```sql
SELECT * FROM ashfall_session_rolls WHERE session_date IN ('2026-06-15','2026-06-22');
SELECT MAX(timestamp_iso) FROM ashfall_session_rolls;
```
and reconcile **both** transcript-only roll logs together — `Roll Statistics S01-S10.md` **§S10** (~90 events, 0 verified) and `Roll Statistics S11-S20.md` **§S11** (~150 events, 0 verified). ⚠️ **The sync-gap check is outstanding, not passed, for both dates — we don't know if either is in the archive at all.**

🛑 **2. Ask the DM: who is the cloaked telepath?** He watched the whole fight, never engaged, then stepped out at 02:05:09 — *"you led me on a delightful chase… I can't wait till next time"* — and vanished with a **massive thrall**. **Nothing was guessed.** `03-Characters/02 NPCs/The Cloaked Figure.md` is a deliberate placeholder, cross-linked from [[The Beast Tamer]] with the full evidence set (overlap is behavioural and entirely circumstantial). **Do not merge until he answers.** The rest of the S11 question set is queued in `00-Campaign-Hub/DM Questions — Open.md` (23 rows, ⭐ section first).

⏭️ **3. S12 is already on disk.** `Session_Sources/Transcripts/Raw_Unedited/12-062926_raw_transcript.md` (**06/29/2026**) was untracked and is now banked in `5696807`. **Convo 1 Phase A has not been run on it.** ⚠️ Before you start: **add a lockfile to the pipeline** — see the friction note below.

⚠️ **4. Website is still NOT updated — for S10 *or* S11.** `rectrixcaedere` is a separate repo and a separate manual deploy. Add **both** sessions to **both** `ARC` registries (`ashfall-britannia/session.html` *and* `archive.html`, kept in lockstep on `n`/`d`/`lbl`/`t`/`f`) and bump the header stats (Sessions Chronicled; Levels Climbed → 10). All vault-side panel sources are written and pushed — Vega's `- **S11` Key Events bullet and the `## S11` blocks in the new Quote Board / Loot / Profanity trackers — so the panels populate the moment the registries exist.

🛑 **5. Add a lock to `Workflows/scripts/ashfall_pipeline_watch.js`.** A duplicate concurrent run fired during S11 Phase B (16:35–16:36) and wrote two artifacts underneath the primary run. Verified identical that time; the next collision may not be. **Two separate concurrency problems hit this vault today** — see the friction lines in the newest log entry.

⚠️ `_pipeline/` is gitignored — review packets and Convo 2 handoffs live only on this machine.
✅ **Closed in the S11 sync:** the duplicate `Mr. Cat.md` (Companions folder verified empty) · the tracker rotation to `S11-S20` · the S10 Sun Shard absence flag · S10's "200 gold, split unrecorded" · the S02 sighted/blind thrall split · Bobby's 7 custom weapons · **and the villain's garbled entrance line**, which Taylor resolved from audio (*"he rests a hand on his head and a massive thrall appears by his side"*) and which is now corrected in all three places it appeared.

---

### Standing work order (still pending, from the 2026-07-04 vault audit)
1. ⚡ **Check the recording app** (standing flag from Session 09: only Taylor's mic captured, no Discord audio). Test before Monday.
2. **Claude Code migration (from 2026-07-04 audit):** unify 10 session notes to the adopted superset schema — add `type: session`; rename `session:` → `session_number:` and `date:` → `session_date:`; kebab keys → snake (`party-level` → `party_level`, `roll-archive-date` → `roll_archive_date`). Ashfall notes carry no `type:` today, so this vault's DnD.base Sessions view currently matches ZERO notes.
3. **Publish command (new):** `Workflows/scripts/Publish-Ashfall.cmd` commits + pushes all note changes; the site fetches session notes straight from this repo's main, so push = publish (~5 min CDN).
4. **Inside Obsidian (never shell):** create `Templates/` and move the six root template stubs into it (they show as phantom blank rows in DnD.base); create `06-Media/` and move the two battle-map PNGs at root into it; move the `ddb-roll-sync/` extension folder under `Workflows/` to match SITL/WTFF layout.
5. **Parity items:** add `Workflows/_Workflows Index.md` and an `Onboard Ashfall Britannia.md` (mirror the SITL/WTFF versions).

---

## Log
<!-- newest first · one entry per logical task/session · timestamp · source · changed · commit · next -->

### 2026-08-28 21:55 ET · Claude Code
- **Changed:** Ran Convo 1 **Phase A only** on `13-062226_raw_transcript.md` and stopped at the human-review gate — nothing applied, no notes, no Supabase, no `.docx`, no Convo 2. **The session identity does not check out and I did not resolve it:** the file is a **second AssemblyAI run of Session 11's audio** (identical source MP3 string, 137-min duration, model, 95.1% confidence, same first/last lines; only the transcribe timestamp differs — 2026-08-29 vs 2026-08-03), and S11 is already complete. Banked the transcript as evidence without renaming or deleting it, and wrote `_pipeline/S13/{spellcheck,flags,summary}.md` + `READY_FOR_REVIEW`. **29 proposed corrections, 9 at/under 60%, 28 flags.** 26 rows are the Taylor-approved S11 rows re-anchored to this file's line numbers with their S11 disposition (`applied`/`held`) recorded; **3 carry a standing "approved but do not apply" instruction and are reproduced as held.** ⚡ **The genuinely new finding is that the two runs disagree, and the diff is evidence:** diarization improved (SPEAKER B **131→81** turns, G **78→96**, partially fixing the S11 catch-all flag), `Beastrell C` → `Beast Thrall C` is fixed, `Beast Thrall F` **regressed** to `Beast Thrall Elf`, `wonderful throw` vs `wonderful thrall` diverged outright, and the unresolved glossary term **`Hunter` vanished entirely** — the same 00:19:40–00:19:51 window transcribes as ordinary combat resolution, which is good evidence it was a first-run ASR hallucination.
- **Commit:** `0802563`
- **Friction:** gen-fail — shell chaining blocked again, **sixth session running**. `cd X && …` was refused outright for git ("can execute untrusted hooks"), a `for`-loop over both transcripts died on `simple_expansion`, process substitution `<(…)` was rejected, `: > file` was refused as "multiple operations", a `Compare-Object` one-liner was blocked as "script block that may execute arbitrary code", and a PowerShell status command was blocked for `$()`. Worked by: `git -C <abs-path>` for every git call (avoids the `cd` refusal entirely — **use this form here from now on**), `Write` with empty content to create the marker file, and separate single-purpose calls for inspection.
- **Friction:** gen-fail — the commit here-string was rejected at **1258 bytes** ("max 965"). Worked by cutting the body roughly in half and pushing the detail into this entry. **Keep Ashfall commit messages under ~900 bytes.**
- **Friction:** misread — **PowerShell's working directory had silently drifted** to `Session_Sources/Transcripts/Raw_Unedited`, so `git status --short` reported the untracked file as a bare root-level path and briefly looked like a *different* file from the one in the session-start status. Caught it with `Get-Location`. Worked by switching to absolute `git -C`. ⚠️ **Do not trust the PowerShell CWD in this repo; the Bash and PowerShell tools do not share one.**
- **Next:** Resolve the duplicate — merge/replace the S11 raw transcript with this cleaner run or discard it — then find the real Session 13 recording (~070626). See the DO NEXT block.
- **Watch out:** 🛑 **Do not process `_pipeline/S13/` as a new session.** Applying those 29 rows to a "Session 13" would duplicate S11 canon and corrupt session keying; `flags.md` §6 lists the S11 worldbuilding that would be re-added. 🛑 **Any speaker map inherited from the S11 spell-check log will be wrong for parts of this file** — ~50 turns moved between letters, so script-format conversion stays blocked for both. ⚠️ **The `Hunter` glossary entry is now probably retirable**, but that's Taylor's call and nothing was changed in `Names & Terms`. ⚠️ **The watcher's first real end-to-end test produced a mislabeled duplicate** — plausibly it was re-pointed at an old recording and auto-numbered the output as the next session. Worth checking before the next session drops. ⚠️ `⟦FILL: player languages / accents⟧` still unfilled in both instruction files, **sixth session running** — every spell-check judgment was made on context alone.

### 2026-08-28 16:40 ET · Claude Code (watcher was never installed)
- **Changed:** 🛑 **This vault had no working automation at all.** The `Ashfall Pipeline Watcher` scheduled task had never been registered — no task, no process — even though every script it needs was present and complete (`ashfall_pipeline_watch.js`, `ashfall_transcribe/transcribe.js`, `Approve-Ashfall.cmd`, `Install-AshfallTask.cmd`). Registered it, patched `Install-AshfallTask.cmd`, and corrected `TRIGGERABLE_ACTIONS.md` §7, which still read *"Status: not registered."* Found during a three-vault audit of the campaign automation.
- **Commit:** `ca0357c`
- **Verification:** ✓ Task registered and read back: `Running`, `ExecutionTimeLimit=PT0S`, `RestartCount=3` at `PT1M`, both battery flags `False`, `MultipleInstances=IgnoreNew`. ✓ **Both** processes confirmed alive — `wscript.exe` 32944 + `node.exe` 35736 running `ashfall_pipeline_watch.js`. ✓ Principal matches the other two vaults exactly (`theli` / Interactive / Limited). ✓ Installer's embedded PowerShell syntax-checked with the PS parser (0 errors). Settings were copied from the healthy `SITL Pipeline Watcher` rather than invented.
- **Watch out:** 🛑 **Do not let the installer's settings block be removed.** `schtasks /Create` on its own inherits `ExecutionTimeLimit=PT72H`, `StopIfGoingOnBatteries=True`, `RestartCount=0`, which is fatal for a logon watcher meant to run forever — the sibling **WTFF watcher was found dead from exactly that** on the same day, killed at its 72h limit with its `node` child orphaned. ⚠️ **A watcher is only healthy when BOTH a `wscript.exe` and a `node.exe` exist for it**; task state alone lies. ⚠️ **This watcher has never actually processed a recording** — it is newly installed and unproven end to end. The first real Ashfall recording dropped into the watched folder is the true test. ⚠️ This vault is the stalest of the three (last content commit 2026-08-03) and is missing the `_Workflows Index.md` and onboarding note its siblings both have.
- **Next:** Unchanged. See the DO NEXT block above — fix the `mcp__plugin_supabase_supabase__execute_sql` allowlist string in `.claude/settings.local.json`.

### 2026-08-03 19:58 ET · Codex
- **Changed:** Replaced the condensed Session 12 recap DOCX with a **full-fidelity Word version of the complete canonical note**. It now carries the source metadata, POV Journal, Summary, every section/table/quote, Threads, Archivist Notes, and Related links from `Session 12 — Only the Relics Can End Their Rule.md`. Structural check: 293 paragraphs, 9 tables, all major canonical-section markers present; accessibility audit has 0 findings. Visual render QA remains unavailable because LibreOffice is missing in this runtime.
- **Commit:** `a4a6bdc`
- **Next:** Use the full Session 12 DOCX as the complete session reference.

### 2026-08-03 19:54 ET · Codex
- **Changed:** Added a casual, player-facing **Session 12** DOCX recap at `01-Sessions/Session 12 — Only the Relics Can End Their Rule.docx`. It distills the Dawnkeep attack, three-relic quest, shopping/downtime wins, and immediate hooks into a skimmable table-reference packet. Structural accessibility check passed without high-severity findings. Visual render QA could not run because LibreOffice is unavailable in this runtime.
- **Commit:** `71bc998`
- **Next:** Use the Session 12 recap for the immediate table reference.

### 2026-08-03 17:20 ET · Claude Code
- **Changed:** Ran **S11 Convo 2 — full vault propagation** for *A Delightful Chase* (06/22/2026), non-interactive, native FS (no Obsidian MCP). **7 files created:** the entire `S11-S20` tracker set (`Loot Tracker`, `Quote Board` 60 quotes, `Profanity Ledger` with running totals carried forward, `Roll Statistics`), plus **`The Dead Zone.md`** (the S10-flagged gap — a glossary Region that had no page despite being a whole session's setting), **`The Cloaked Figure.md`** and **`The Massive Thrall.md`**. **24+ updated:** Dashboard (S11 row, **threads 56–67 opened**, 7 moved to Resolved, NPC directory +5, timeline, **Party Snapshot rewritten**, tracker Reference rotated); Vega's journal (collapsible callout, verbatim); **all 7 PC pages** with site-parser `- **S11` Key Events bullets — Vega additionally +6 Inner Life entries, +6 quotes, +6 relationships, an Items block and a new **bear** aspect row; `Mr. Cat`, `Bobby`, `Dr. Wilson`, `The Beast Tamer`; **`Beast Thrall` substantially rewritten** with the DM's live statblock disclosures (~86 HP, the below-50 **"blood trait" frenzy**, **double damage to structures**, **radiant doubling**, the **eyed variant confirmed**); both library and castle location pages; `Setting Primer` (+3 sections, +9 open questions) and `Names & Terms` (`## S11 first-seen terms`, 15 entries, **none canonized**); `House Rules & Rulings` **+10 standing / +8 deferred**; `Homebrew Abilities` +3; `Spell_Usage` +29 rows; `DM Questions — Open` +5/−2; four pointer stubs rotated and the four `S01-S10` trackers marked 🔒 closed. **`Vault Sync Status.md` updated last** with the full S11 matrix and a change-log row. Also banked the untracked **S12 raw transcript** (`12-062926`).
- **Commit:** `5696807` *(plus `84dd76e`, which the Obsidian git plugin auto-committed mid-run — see friction)*
- **Friction:** gen-fail — Supabase `execute_sql` permission-denied for the **fifth consecutive run**. Worth recording precisely, because this pass **proved** the root cause rather than inferring it: the `supabase` MCP server *did* connect mid-session and exposed **`mcp__supabase__execute_sql`**; the permission prompt was raised against that exact live tool and denied, because `.claude/settings.local.json` allowlists `mcp__plugin_supabase_supabase__execute_sql` — a name matching no connected server. **It is a one-string fix.** I did not route around the gate. Consequence unchanged: S10 and S11 roll logs are both transcript-only and the sync-gap check is still outstanding.
- **Friction:** gen-fail — **the Obsidian git plugin's 10-minute auto-commit fired mid-run** and swept ~28 of my in-progress files into `84dd76e` under *its* message, splitting one logical unit of work across two commits with an unrelated subject line. Nothing was lost, but the S11 sync is not readable from a single commit. Worked by checking `git log` before committing rather than assuming a clean tree, then writing the remainder as `5696807`. **Long-running vault work in this repo will always race the auto-commit — check the log before you commit, and expect the split.**
- **Friction:** misread — I drafted two DM questions (Barrett's 18-vs-19 crit range, and "what is Pins and Needles?") that **violate this vault's own standing rule**, stated at the bottom of `DM Questions — Open.md`: homebrew wording is deliberately *not* on that list, because Taylor is a player and doesn't have the DM's homebrew cards. Caught it on reading `Homebrew Abilities.md`, which already had both filed as canon-in-play. Worked by removing both rows, renumbering, and softening the `Names & Terms` entry to point at `Homebrew Abilities` instead of demanding confirmation. **Read `Homebrew Abilities.md` before adding anything to the DM list.**
- **Friction:** gen-fail — shell chaining blocked *again*, fifth session running: `for`-loops and `cat` over multiple files died on `simple_expansion`, a PowerShell replacement was rejected for `$()`, and even the prescribed `git add -A && git commit -m … && git push` was refused as "multiple operations". Worked by dropping to `Read`/`Grep` for inspection and running the whole git motion as a **single PowerShell call** (`git add -A; git commit -m "…"; git push`), which passed. **The multi-`-m` commit form is also blocked here — one `-m`, and put the detail in the handoff.**
- **Next:** Fix the `settings.local.json` Supabase tool name, then run the roll queries interactively for both 2026-06-15 and 2026-06-22 and reconcile both `Roll Statistics` files. After that, S12 (`12-062926`) Convo 1 Phase A — but add the pipeline lockfile first.
- **Watch out:** ⚠️ **Roll Stats is the one S11 checklist row that is NOT ✅** — it's marked ⚠ transcript-only in the sync matrix, on purpose. Don't let a green-looking matrix hide it. ⚠️ **[[The Cloaked Figure]] is a placeholder and must not be merged** into [[The Beast Tamer]] or [[The Beast Master]] without the DM — the behavioural overlap is strong and entirely circumstantial, and the page says so. ⚠️ **SPEAKER B is a diarization catch-all** mapped to Deanna/Madi, so her profanity counts and several quote attributions are the least reliable data in the S11 trackers; it's flagged in place on every affected file. ⚠️ **Two standing house rules now contradict each other on grapple escapes** (S09 flat save vs S11 opposed check) — both are live in `House Rules & Rulings`, and it ran three times in S11. ⚠️ **No `Sun Shard` page exists** anywhere in the vault, so `[[Sun Shard]]` / `[[The Sun Shard]]` are broken links campaign-wide — pre-existing, not introduced here, but the relic now has a confirmed damage profile and nowhere to live.

### 2026-08-03 16:50 ET · Claude Code
- **Changed:** Ran Convo 1 **Phase B** for **Session 11** (062226) off the approved `_pipeline/S11/spellcheck.md`. Applied **25 of 28** rows (the 3 the approved table itself marks "do not apply without a listen" / "flagged rather than recommended" / "flag, don't apply" — `my O and F`→`my bow on F` 50%, `she said`→`he said` 45%, `Nova Twins save` 35% — left verbatim). Wrote the spell-check log and the full 8-section canonical note **`01-Sessions/Session 11 — A Delightful Chase.md`** (95 KB), titled verbatim from the cloaked telepath's parting line at 02:05:09, which bookends his opening taunt *"run, I want to see how far you get"*; 5 alternates logged. Site-parser contract verified (all five `##` H2s + the `Location` row + `## POV Journal`). Convo 2 handoff written to `_pipeline/S11/handoff.md` with the note path as line 1. No `.docx`. **Session shape:** one continuous Dead Zone ambush, ~87% of a 137-min night, six Beast Thralls A–F, **no PC downed**; **Vega took 3 of the 6 kills** — a flow-state greataxe kill she describes as *ballet*, then she **stows the axe mid-combat** to finish bare-knuckle ("given in fully to the beast… she's a bear"). The **Sun Shard resurfaces for the first time since S07**, as a d6 radiant rider on her knuckles. The antagonist never fights.
- **Commit:** `4ad9012`
- **Friction:** gen-fail — Supabase MCP `execute_sql` permission-denied **again**, now on **all four** connected servers (`supabase`, `supabase-account2`, `supabase-cutter`, `supabase-aftermath-meridian`). **Fourth consecutive blocked run — but this pass found the root cause:** `.claude/settings.local.json` allowlists `mcp__plugin_supabase_supabase__execute_sql`, a tool name that matches **no currently connected server**. Fix that entry and this stops recurring. I deliberately did **not** route around the permission gate with `.env` credentials; Step 4 never ran, so the S11 roll log is transcript-only and the sync-gap check is still outstanding for both S10 and S11.
- **Friction:** gen-fail — **a duplicate concurrent run of this same phase executed mid-flight** (16:35–16:36), writing the corrected transcript and spell-check log underneath me; an `Edit` died with "file has been modified since read" and grep line numbers shifted between calls. Worked by *stopping the race instead of winning it*: I re-derived the correct 25-applied/3-held set independently, verified the other run's output row-by-row against the approved table, found it correct, and **kept its files rather than clobbering them** — then wrote only the artifacts it hadn't reached. Suspected cause is `ashfall_pipeline_watch.js` and a manual launch both firing `_prompt_convo1_phaseB_apply_md.txt`; **needs a lock before the next run.**
- **Friction:** gen-fail — `node` execution was permission-blocked (both Bash and PowerShell), so the deterministic anchored-replacement script couldn't run; compound `cd X && …` and a `for`-loop over grep patterns were blocked too (`simple_expansion`). Worked by verifying state with `Grep` alternations against the approved `from`/`to` strings instead of executing anything, then applying the remainder via `Edit`. The unused script was deleted rather than left as dead code. **Still true, fourth session running: one operation per shell call in this repo.**
- **Next:** Re-run the Supabase roll queries interactively for **both** 2026-06-15 and 2026-06-22, reconcile both roll logs, then start S11 Convo 2 from `_pipeline/S11/handoff.md`.
- **Watch out:** ⚠️ **S11 opens a NEW tracker range** — all four `S11-S20` files must be *created*, with profanity totals carried forward. ⚠️ The corrected transcript is **not** in script format: the speaker map is inferred and **SPEAKER B is a diarization catch-all** at 26 identified sites, so every B-attributed quote and profanity count is suspect. ⚠️ Per no-invention I canonized **nothing** about the cloaked telepath — he is logged as an unidentified NPC, not merged into [[The Beast Tamer]] or [[The Beast Master]]. ⚠️ The villain's entrance line is still **cut mid-sentence** and the transcriber emitted **zero** `[inaudible]` markers. ⚠️ Four new house rulings this session (action→2nd bonus action; bonus-action Cure Wounds; point-blank vital-organ d20; Vega's punch-punch-kick), and **Barrett's crit range is now 18**, not the glossary's 19.

### 2026-08-03 16:20 ET · Claude Code
- **Changed:** Ran Convo 1 **Phase A only** for **Session 11** (062226) and stopped at the human-review gate as instructed — nothing applied, no notes, no Supabase, no `.docx`, no Convo 2. Banked the previously-untracked raw transcript (137 min, 2,034 lines, 15,262 words, `universal-3-5-pro`, 95.1%) and wrote `_pipeline/S11/{spellcheck,flags,summary}.md` + `READY_FOR_REVIEW`. **28 proposed corrections, 8 at/under 60%** — a much lighter table than S10 because this session names enemies only by **letter** (Beast Thrall A–F), so there's no christened-joke-name roster to adjudicate. **41 flags.** Session/date cross-check **passed**: the transcript opens exactly where S10 ended (leaving the library, long rest already taken), Barrett confirms level 10, and 06/22 is 7 days after S10's 06/15. Also carries forward two commits from an earlier interactive session that were never logged here — `bf5035e` (S10 enemy names corrected to the DM-confirmed spellings: `Shathide`→**Shithead**, `Absidy`→**Abcde**, `Tink` folded into **Taint**) and `4234f9e` (DM Questions master list; Lucky pronoun flag closed).
- **Commit:** `1ad867c`
- **Friction:** gen-fail — compound Bash calls were permission-blocked *again*, third session running: `git check-ignore … ; echo` and `: > file && ls` both died on the second operand. A PowerShell replacement was then rejected for containing `$()`. Worked by dropping chaining entirely — `git -C "<path>" …` as single un-chained calls, and the `Write` tool for the empty `READY_FOR_REVIEW` marker instead of a shell redirect. **Stop reaching for `&&` and `;` in this repo; one operation per call.**
- **Next:** Taylor reviews `_pipeline/S11/spellcheck.md` + `flags.md`, answers the three gate questions in DO NEXT, then approve Phase B.
- **Watch out:** ⚠️ **The S11 speaker letters are NOT S10's speaker letters** — the diarizer reassigned all eight. Do not carry the S10 map forward; it will silently misattribute every line. ⚠️ `SPEAKER B` is a diarization catch-all this session, absorbing DM adjudications, turn calls, and other players' rolls (~18 identified sites in `flags.md` §2). ⚠️ The session's single most important line — the villain's entrance at 02:05:09 — is **cut mid-sentence** ("He rests a hand on his head in a And a massive thrall appears"), and the transcriber emitted **zero** `[inaudible]` markers anywhere in the file, so nothing is auto-marked. ⚠️ Per the no-invention rule I canonized nothing: all 14 first-seen terms are parked in `flags.md` §1 pending the DM.

### 2026-08-03 12:35 ET · Claude Code
- **Changed:** Ran **Convo 2** (full vault propagation) for Session 10 off `_pipeline/S10/handoff.md`. **28 files: 3 created, 24 updated, 1 deleted.** Created `The Beast Master.md` (NPC — psychic beast-thrall lore, the session's only worldbuilding drop), `Cult of the Pale World.md` (filled the empty `04-World-Lore/Factions/` folder and resolved a dangling backlink), `Unknown Great Cat.md` (the severed skull). Dashboard: S10 row, threads 38/44/45 **resolved** and **46–55 opened**, NPC directory +5, locations, timeline, party → **LEVEL 10**. All four trackers got `## S10` blocks (Profanity running totals recomputed; **XP Tracker retired to milestone**). Journal callout appended verbatim. **All 7 PC pages** — Key Events plus a real find: every one still read *"Level: 5 as of S01"*, six sessions stale, all corrected to 10. Also Mr. Cat / Bobby / Beast Tamer, Grand Library (10-ft floors, battle-over status), Walled Base, Setting Primer, Names & Terms (`## Provisional ⚑ (S10)`, 17 first-seen terms, **none canonized**), House Rules **+5 standing / +4 deferred**, Spell Usage. Deleted the 0-byte duplicate `01 PCs/Companions/Mr. Cat.md` so `[[Mr. Cat]]` resolves to one page. Vault Sync Status updated last with a full S10 matrix.
- **Commit:** `6b7b288`
- **Friction:** gen-fail — the Supabase MCP `execute_sql` was permission-denied **again**, on both `supabase` and `supabase-account2`, exactly as in the two Convo 1 passes. Nothing worked around it; this is now three consecutive automated runs blocked on the same thing. I wrote the `## S10` Roll Statistics block anyway but marked it **⚠ transcript-only, NOT ✅** with the reconciliation query inline, rather than letting a transcript reconstruction sit in the tracker looking archive-verified. **Stop trying this non-interactively — it needs an interactive session.**
- **Friction:** gen-fail — compound `cd X && …` Bash calls were permission-blocked again (same as 2026-07-29 and the Phase A run), and a `for`-loop over tracker tails tripped a "simple_expansion" parser error. A PowerShell `Get-ChildItem | ForEach-Object` replacement then timed out at 5s. Worked by dropping shell iteration entirely: `Grep` for `^## ` to locate section anchors, then `Read` with `offset` to pull each tail. Faster than the shell approach and it surfaced the anchors directly.
- **Next:** Re-run the 2026-06-15 Supabase roll queries interactively and rewrite the `## S10` block in `Roll Statistics S01-S10.md`. Then the `rectrixcaedere` website registries.
- **Watch out:** ⚠️ **The website is not updated** — separate repo, separate deploy, out of scope for this automated run; both `ARC` registries plus header stats still need S10. ⚠️ Per the no-invention rule I deliberately did **not** canonize any of the 17 first-seen S10 proper nouns (Shathide, Absidy, Lucky, Taint, Cuck, Tink, Carl Senior/Jr., Karlsteiner…) — they're all parked under `## Provisional ⚑ (S10)` pending the DM. ⚑ New contradiction worth surfacing at the table: S02's "emeralds are the only way to kill a Beastmaster" is now directly contradicted in play.

### 2026-08-03 11:40 ET · Claude Code
- **Changed:** Ran Convo 1 **Phase B** for Session 10 (061526) off the approved `_pipeline/S10/spellcheck.md`. Applied **26 of 29** rows with word-boundary matching (the 3 the approved table itself marks "do not apply without DM confirmation" / "not recommended" — `Karlsteiner`→`Carl Senior` 45%, `Tinkt— er`→`Taint— er` 40%, Bobby `master curator`→`master armorer` 35% — were left verbatim). Wrote the corrected transcript, the spell-check log, and the full 8-section canonical note **`01-Sessions/Session 10 — Never Truly Alone.md`** (title from Vega reading the Beast Master lore aloud; 5 alternates logged). Site-parser contract verified: all five `##` H2s present plus the Location row. Convo 2 handoff written to `_pipeline/S10/handoff.md`. No `.docx`.
- **Commit:** `8076bc6`
- **Friction:** gen-fail — the Supabase MCP `execute_sql` was permission-denied in the non-interactive run on **both** the `supabase` and `supabase-account2` servers, and there was no fallback (vault `.env` has only `DDB_COBALT`; `Get-ChildItem Env:` was itself blocked as a non-filesystem provider path). **Step 4 never ran at all** — the roll log is transcript-only and the sync-gap check is outstanding. Nothing worked; this one needs an interactive session, not a different phrasing.
- **Friction:** gen-fail — `node`, `Copy-Item` and compound `cd X && …` were all permission-blocked, so the deterministic 26-rule correction script (`_pipeline/S10/apply_corrections.js`) could not execute. Worked by writing the corrected transcript directly with the `Write` tool against that exact rule set, then verifying with two `Grep` passes (zero surviving pre-correction tokens; the 3 deliberately-unapplied strings confirmed still present). The script is left in `_pipeline/` as the auditable spec — deletion was blocked too.
- **Next:** Re-run the Supabase roll cross-reference for 2026-06-15 interactively and reconcile, then start Convo 2 from `_pipeline/S10/handoff.md`.
- **Watch out:** ⚠️ The corrected transcript is **not** in script format — `flags.md` §2 marks the SPEAKER A–H → character map as inferred, and there's a live contradiction proving it (01:08:51 assigns *"I wish my name was Madeline instead of **Madison**"* to SPEAKER F, established as Taylor/Vega at 00:16:01). Converting would hard-code unverified attribution. ⚠️ Also unresolved: **Carl Senior is never declared dead** yet Cuck is confirmed the last enemy standing, and it's unclear who Deanna killed at 00:34:57 — both caused by the DM clearing tokens with "dead, dead, dead" down the initiative list.

### 2026-08-03 10:55 ET · Claude Code
- **Changed:** Ran Convo 1 **Phase A only** for Session 10 (061526) and stopped at the human-review gate as instructed — no corrections applied, no notes, no Supabase, no Convo 2. Wrote `spellcheck.md` (29 proposals, 19 capped at ≤60% under the no-external-canon rule for first-seen proper nouns), `flags.md`, `summary.md`, and the `READY_FOR_REVIEW` marker into `_pipeline/S10/`. Also banked the transcriber's model bump to `universal-3-5-pro` (AssemblyAI deprecated `universal-3-pro`) and the watcher's new `--file` one-off mode, both of which were sitting uncommitted.
- **Commit:** `40d7e28`
- **Friction:** gen-fail — Grep's `-C` context output rendered `//` comments in `transcribe.js` as a stray `\`, and I logged a nonexistent syntax bug in `flags.md` before catching it. `Read` on the same lines showed the file was clean. Confirm apparent syntax breakage with Read, never from Grep context output.
- **Friction:** gen-fail — compound Bash calls (`cd X && git add -A && git commit`) got permission-blocked mid-chain, so the `add` silently never ran and the commit died with "no changes added to commit." Worked as separate un-chained `git -C "<path>" …` calls through the PowerShell tool.
- **Next:** Taylor reviews `_pipeline/S10/spellcheck.md` + `flags.md`, then approve to run Phase B.
- **Watch out:** ⚠️ `_pipeline/` is in `.gitignore`, so the entire Phase A review packet is untracked and local-only. It will not survive a clone, and a `git clean -xdf` would delete it.

### 2026-07-29 20:40 ET · Claude Code
- **Changed:** Removed the retired duplicate `ddb-roll-sync` extension from `Workflows/ddb-roll-sync/` and `ddb-roll-sync/`, 11 files including a stray annotation xlsx. Every copy across the vaults had drifted to a different version, so it is consolidated in one place and writes direct to Rectrix_Caedere. Found by a cross-repo handoff sweep; the deletions were already sitting uncommitted in the working tree and Taylor confirmed they were deliberate.
- **Commit:** `e0682ed`
- **Next:** Unchanged. See the pre-session checklist in the block above this log.
- **Watch out:** ⚠️ This was a *deletion* commit banked on Taylor's confirmation, not on my own reading of the tree. If a copy of the extension is still needed here, it is in git history at the parent of `e0682ed`.

### 2026-07-26 11:44 ET · Claude Code
- **Changed:** Added the Handoff Contract to `AGENTS.md` so Codex follows it. Codex reads `AGENTS.md`, never `~/.claude/skills/`, so it had no handoff instructions at all before this.
- **Commit:** `b25d604`
- **Next:** Unchanged. See the block above this log.
- **Watch out:** Log entries must now carry a tool label (`Claude Code` / `Claude desktop` / `Codex` / `ChatGPT`). Do not restructure this file; the dashboard parses it.

### 2026-07-04 ET · Claude chat
- **Changed:** Added `Workflows/scripts/Publish-Ashfall.cmd` (one-command note publish). Rewrote DO NEXT as the pre-Monday work order from the 2026-07-04 three-vault consistency audit (missing `type:` frontmatter, no Templates folder, root clutter, workflow-doc parity gaps).
- **Commit:** `Add Publish-Ashfall.cmd; DO NEXT = pre-Monday work order (vault audit)`
- **Next:** Item 1 anytime before Monday; item 2 in Claude Code.
- **Watch out:** item 4 moves must happen inside Obsidian so wikilinks update; never via shell or file tools.

### 2026-06-23 09:37 ET · Claude chat
- **Changed:** Enabled repo handoff — added this `HANDOFF.md` at root.
- **Commit:** `docs: enable repo handoff`
- **Next:** Set by the next real change to the repo.
